---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - method/00-identity.md
  - method/10-philosophy.md
  - method/20-tools.md
  - method/30-quiz-options.md
  - method/40-discipline.md
  - method/50-checks.md
  - method/60-process.md
  - method/70-material.md
  - method/80-library.md
  - method/90-preferences.md
  - method/95-writing-style.md
  - method/surfaces/app.md
  - method/surfaces/claude-code.md
  - method/surfaces/codex-learn.md
  - method/surfaces/codex-review.md
  - scripts/render-method.mjs
  - scripts/check-method.mjs
  - server/src/method.generated.ts
  - server/src/prompt.ts
  - plugin/skills/teach/SKILL.md
  - plugin/commands/learn.md
  - plugin/commands/review.md
  - codex/skills/derive-learn/SKILL.md
  - codex/skills/derive-review/SKILL.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [FOUND-02, FOUND-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-01 the canonical method lives in Markdown under method/, split by section, and renders to the app system prompt, plugin/skills/teach/SKILL.md, both Codex SKILL.md bodies and the plugin commands' allowed-tools lists."
    - "Per D-02 one surface-neutral method body is shared verbatim by every target; only a short hand-written preamble per surface differs, carrying MCP tool prefixes, terminal-versus-browser answering and the start_lesson/end_lesson lifecycle. The shared body contains no conditional blocks — only placeholder tokens in the style of the existing {{WEB_TOOLS}}."
    - "Per D-03 the method body is the union of the three existing copies with server/src/prompt.ts as the spine: every rule any copy states survives, including the sections only plugin/skills/teach/SKILL.md carries (course material, the learner's library, what a check tells you, the warm-up phase, the preference rules) and the two operational lines only plugin/commands/learn.md carries."
    - "Per D-03 nothing in the method is compressed, shortened or reworded to save tokens; the longer prompt is the accepted cost of this refactor."
    - "The union rule has no passable exception: a rule missing from the merged body fails task 2 outright. Recording an omission with a reason in the summary is not a remedy and does not convert a miss into a pass; a genuinely unavoidable omission stops the task and goes to the developer as a blocking question."
    - "Every pre-phase comparison in this plan resolves against the literal commit b8f255cee3bd601a081df8335c82e6212d97e62e, not HEAD or HEAD~N, so the rule inventory cannot pass vacuously against a partly-migrated copy."
    - "Per D-04 every rendered copy is committed to git and pnpm method:check fails when any of them drifts from method/; CI runs that check with no `|| true`."
    - "Per D-07 the allowed-tools frontmatter of plugin/commands/learn.md and plugin/commands/review.md is generated from the tool registry under the same commit-and-CI-fails-on-drift policy, while the prose body of each command file stays hand-written."
    - "pnpm method:check on an unmodified tree exits 0; changing one character of any rendered copy, or deleting any method/*.md section, makes it exit non-zero and name the file."
    - "The drift check compares rendered bytes to the committed copy exactly — UTF-8, LF line endings, one trailing newline — so a trailing-whitespace or CRLF change fails."
    - "Rendering with a method/ section missing fails with a named error rather than silently emitting a target without that section."
    - "Codex skills gain full generated SKILL.md bodies; today they have none and teach on tool descriptions alone, which is a real method gap."
  artifacts:
    - method/10-philosophy.md
    - method/surfaces/claude-code.md
    - scripts/render-method.mjs
    - scripts/check-method.mjs
    - server/src/method.generated.ts
    - codex/skills/derive-learn/SKILL.md
    - codex/skills/derive-review/SKILL.md
  key_links:
    - "method/*.md -> scripts/render-method.mjs -> server/src/method.generated.ts -> server/src/prompt.ts systemPrompt()"
    - "method/*.md + method/surfaces/*.md -> plugin/skills/teach/SKILL.md"
    - "method/*.md + method/surfaces/*.md -> codex/skills/derive-{learn,review}/SKILL.md"
    - "server/test/wire-surface.json (written by plan 01-01 in wave 1; read-only here and not in any other wave-2 plan's files_modified) -> generated allowed-tools in plugin/commands/*.md"
    - "scripts/check-method.mjs -> .github/workflows/ci.yml (the failure D-04 asks for)"
  prohibitions:
    - "No rule present in any of the three existing method copies is dropped, shortened or reworded to save tokens during the merge; the method text is the product, and a refactor phase is the worst possible place to lose a rule."
    - "No conditional block, no per-surface branch and no per-surface omission inside the shared method body; per-surface variation lives only in the short hand-written preambles, so a diff shows at a glance that the body is identical everywhere."
    - "The rendered copies are never auto-regenerated-and-pushed by CI; a method change must be seen as a reviewable diff in the pull request."
    - "No UI control, panel or page is added."
  planner_assumptions:
    - "Planner's call, recorded: the app target is a generated TypeScript module server/src/method.generated.ts rather than a runtime read of method/*.md. Reason: the release tarball and the tsc output must be self-contained, and D-04 already commits every rendered copy. If a runtime read is preferred, this is the assumption to correct."
    - "Planner's call, recorded: scripts/render-method.mjs reads the tool name list from the committed server/test/wire-surface.json rather than importing the TypeScript registry. Reason: scripts/ has no package.json and must run on bare Node with only node: imports, and the wire-surface fixture is the registry's committed projection with its own test guaranteeing they agree. If the render step should instead run through tsx from the server package, this is the assumption to correct."
---

<objective>
Make the teaching method exist once. Today it is written three times — the
`PROMPT` template in `server/src/prompt.ts` (175 lines), the 166-line
`plugin/skills/teach/SKILL.md`, and a third partial retelling in
`plugin/commands/learn.md` — and CONCERNS.md records them already drifting.
Meanwhile the two Codex skills carry no method at all: eight lines of manifest
each, teaching on tool descriptions alone.

Per D-01 the canonical text moves to Markdown under `method/`. Per D-02 one
surface-neutral body is shared verbatim and only a short preamble differs per
surface. Per D-03 the merged body is the **union** of the three copies with
`prompt.ts` as the spine — every rule survives, nothing is compressed. Per D-04
the rendered copies are committed and CI fails on drift. Per D-07 the
`allowed-tools` frontmatter is generated from the same registry by the same
render step, which is what makes the phase's second success criterion hold by
construction rather than by a second mechanism.

Purpose: FOUND-02, plus the `allowed-tools` half of FOUND-01.
Output: `method/`, two scripts, five rendered targets, one CI gate that fails.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

## Pre-phase reference commit

`PRE = b8f255cee3bd601a081df8335c82e6212d97e62e`

Every "what did the method say before the phase" comparison in this plan resolves
against `PRE` — never `HEAD`, never `HEAD~N`. This is load-bearing, not tidiness:
waves 1 and 2 land commits before task 2 runs, and **task 1 of this very plan
rewrites `server/src/prompt.ts` and `plugin/skills/teach/SKILL.md`**. A relative
reference would compare the merged method against a partly-migrated copy of itself
and report a vacuous pass on the one check that protects the method text.

Assert `PRE` is the right commit before trusting any comparison below:

    git show $PRE:server/src/prompt.ts            | wc -l   # 175
    git show $PRE:plugin/skills/teach/SKILL.md    | wc -l   # 166
    git show $PRE:plugin/commands/learn.md        | wc -l   # 18
    git show $PRE:plugin/commands/review.md       | wc -l   # 11

If any line count differs, stop and raise it. The reference commit is wrong, and
every union check in task 2 is meaningless until it is right.

## Read-only input from another plan

`server/test/wire-surface.json` is **written by plan 01-01 (wave 1)** and only
read here. Plan 01-02 shares this wave but does not write it — it is contractually
forbidden to (its task 1 verify asserts `git diff --quiet` on that path), and the
file is not in its `files_modified`. So the ordering this plan depends on is the
declared `depends_on: ["01-01"]` edge, with no undeclared same-wave edge behind it.

Before rendering, assert the fixture is the finished one: its `mcp` list has 22
entries. If it has fewer, plan 01-01 did not complete and the render must stop
rather than emit a short `allowed-tools` line.

## Artifacts this phase produces

Created by **this plan**: the `method/` directory (11 body sections + 4 surface
preambles); `scripts/render-method.mjs` and `scripts/check-method.mjs`; the root
scripts `method` and `method:check`; `server/src/method.generated.ts` exporting
`METHOD_SECTIONS` and `METHOD_BODY`; `codex/skills/derive-learn/SKILL.md` and
`codex/skills/derive-review/SKILL.md`; the `method:` region markers inside
`plugin/skills/teach/SKILL.md`; a `Method text has not drifted` CI step.

Consumed from plan 01-01 (new this phase, not drift): `server/test/wire-surface.json`.

<tasks>

<task type="tracer">
  <name>Task 1: one method section, rendered to every target, with CI failing on drift</name>
  <reversibility rating="costly">Per D-01, moving the canonical form away from Markdown later means re-authoring the render pipeline and every downstream target; the text itself stays portable.</reversibility>
  <read_first>
    - server/src/prompt.ts in full (175 lines — the PROMPT template that is the spine, the {{WEB_TOOLS}} placeholder at line 39, systemPrompt at 106-109, and the four turn-prompt functions at 116-175 that must stay where they are)
    - plugin/skills/teach/SKILL.md in full (166 lines — the second copy, and its frontmatter shape)
    - plugin/commands/learn.md and plugin/commands/review.md in full (the third partial retelling and the two allowed-tools lines)
    - codex/skills/derive-learn/agents/openai.yaml and codex/skills/derive-review/agents/openai.yaml (8 lines each; no SKILL.md body exists yet)
    - scripts/doctor.mjs lines 1-30 (the .mjs script pattern: shebang, prose doc block, node:-only imports, collector functions, repo root resolved from import.meta.url)
    - server/test/wire-surface.json (the committed tool-name projection the render step reads)
    - .github/workflows/ci.yml in full
    - .planning/phases/01-foundation/01-PATTERNS.md section "method/*.md + render script (D-01..D-04)"
  </read_first>
  <files>method/30-quiz-options.md, scripts/render-method.mjs, scripts/check-method.mjs, server/src/method.generated.ts, server/src/prompt.ts, plugin/skills/teach/SKILL.md, codex/skills/derive-learn/SKILL.md, codex/skills/derive-review/SKILL.md, package.json, .github/workflows/ci.yml</files>
  <action>
Stand the whole pipeline up on one section, so the architecture is proven before the full method moves.

Create `method/30-quiz-options.md` holding the "Writing quiz options (construction procedure, every time)" section, taken as the union of the two existing copies with `server/src/prompt.ts`'s five numbered steps as the spine and the closing line "If you can tell which option is right without knowing the material, regenerate." Write it as plain Markdown with no frontmatter; the first line is the section's own heading.

Create `scripts/render-method.mjs` following the `scripts/doctor.mjs` pattern exactly: `#!/usr/bin/env node` shebang, a prose `/** */` doc block saying what it renders and why the copies are committed, imports only from `node:` modules, repo root resolved from `import.meta.url`. It reads every `method/*.md` in filename order into an ordered map keyed by the filename stem with the numeric prefix stripped, reads `method/surfaces/*.md`, reads the ordered tool-name list from `server/test/wire-surface.json`, and writes five targets. Missing input is a hard failure: if `method/` is empty, if a section file is zero bytes, or if a surface preamble names a section id that no file provides, throw a lowercase sentence naming the file and exit non-zero — a target silently missing a section is exactly the failure D-04 exists to prevent.

Render into **marked regions** so a half-migrated tree is never a broken tree. In `plugin/skills/teach/SKILL.md` the generated span sits between an HTML comment open marker and its matching close marker; the render replaces only what is between them and leaves the hand-written remainder untouched. In `server/src/method.generated.ts` the whole file is generated: a prose `/** */` header saying it is generated by `pnpm method` and must not be hand-edited, then a named export `METHOD_SECTIONS` mapping section id to its text and a named export `METHOD_BODY` joining them in order — named exports only, no default export, matching the project's rule. In the Codex `SKILL.md` files the whole file is generated. In `plugin/commands/learn.md` and `plugin/commands/review.md` only the `allowed-tools:` frontmatter line is replaced.

For this task, put exactly the one section into the generated span of each target: `server/src/method.generated.ts` carries `quiz-options`; `plugin/skills/teach/SKILL.md` keeps every other line it has today and its existing quiz-options prose is replaced by the marked region; `codex/skills/derive-learn/SKILL.md` and `codex/skills/derive-review/SKILL.md` are created with a hand-written frontmatter block plus that region — they have no body today, so one real section is a strict improvement rather than a regression.

Change `server/src/prompt.ts` to import `METHOD_SECTIONS` from `./method.generated.js` and splice the quiz-options section into the `PROMPT` template in place of the inline copy, leaving `{{WEB_TOOLS}}`, `systemPrompt(backend)` and the four turn-prompt functions exactly as they are.

Create `scripts/check-method.mjs`, same script pattern: it renders into a directory made with `mkdtempSync`, byte-compares each rendered target against the committed one, prints a unified-diff-style report naming every file that differs, removes the temp dir, and exits non-zero if anything differed. Compare bytes, not parsed content: UTF-8, LF endings, exactly one trailing newline, so a whitespace or CRLF change fails.

Add the root scripts `method` (runs the render) and `method:check` (runs the check) to `package.json`.

Add a CI step to `.github/workflows/ci.yml` named `Method text has not drifted`, running `pnpm method:check`, placed after Install. Do not copy the `|| true` from the neighbouring doctor step: D-04 asks for a failure, and a method change must be seen. Leave the doctor step exactly as it is.
  </action>
  <verify>
    <automated>pnpm method:check</automated>
    <fails_when>non-zero exit, or the output names any file as differing</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>pnpm build &amp;&amp; pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0</fails_when>
    <automated>node --check scripts/render-method.mjs &amp;&amp; node --check scripts/check-method.mjs</automated>
    <fails_when>non-zero exit, or any SyntaxError is printed</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm method:check` exits 0 on a clean tree.
    - Appending one space to the end of a line inside the generated region of `plugin/skills/teach/SKILL.md` makes `pnpm method:check` exit non-zero and print that file's path; reverting makes it exit 0.
    - Truncating `method/30-quiz-options.md` to zero bytes makes `pnpm method` exit non-zero with a lowercase message naming that file; restoring it makes the render succeed.
    - `codex/skills/derive-learn/SKILL.md` and `codex/skills/derive-review/SKILL.md` exist, each with frontmatter and the quiz-options section.
    - `server/src/method.generated.ts` uses named exports only and contains no `export default`.
    - `.github/workflows/ci.yml` contains a step running `pnpm method:check`, and that step's `run:` line does not end with the two-character sequence that suppresses a failure (verify with `grep -A1 "Method text has not drifted" .github/workflows/ci.yml` showing a bare `pnpm method:check`).
    - The rendered app system prompt (`METHOD_SECTIONS['quiz-options']`) contains the sentence "If you can tell which option is right without knowing the material, regenerate."
    - `pnpm build && pnpm test` passes with 0 failures.
  </acceptance_criteria>
  <done>One section of the method is authored once and lands byte-identically in five committed copies; CI fails when any of them drifts, and the plugin, the Codex skills and the app all still work.</done>
</task>

<task type="auto">
  <name>Task 2: author the full merged method body — the union, nothing lost</name>
  <read_first>
    - server/src/prompt.ts via `git show $PRE:server/src/prompt.ts` (the spine, in its pre-phase form; `$PRE` is the reference commit named above — task 1 has already rewritten the working-tree copy, so `HEAD` is the wrong source here)
    - plugin/skills/teach/SKILL.md via `git show $PRE:plugin/skills/teach/SKILL.md` (the second copy, in its pre-phase form — read all 166 lines)
    - plugin/commands/learn.md via `git show $PRE:plugin/commands/learn.md` (steps 4 to 6 carry the two operational lines only this file has)
    - method/30-quiz-options.md and scripts/render-method.mjs (as left by task 1)
    - .planning/phases/01-foundation/01-CONTEXT.md section "Method text: one source" (D-01 through D-04) and the "Specific Ideas" note that the method text is the product
  </read_first>
  <files>method/00-identity.md, method/10-philosophy.md, method/20-tools.md, method/30-quiz-options.md, method/40-discipline.md, method/50-checks.md, method/60-process.md, method/70-material.md, method/80-library.md, method/90-preferences.md, method/95-writing-style.md, scripts/render-method.mjs, server/src/method.generated.ts, server/src/prompt.ts, plugin/skills/teach/SKILL.md, codex/skills/derive-learn/SKILL.md, codex/skills/derive-review/SKILL.md</files>
  <action>
Write the whole merged method body into `method/`, section by section, as the union described in D-03 with `server/src/prompt.ts` as the spine.

Create these section files, each plain Markdown, each opening with its own heading, numbered so filename order is reading order: `00-identity.md` (the opening "You are Derive…" statement and the goal-is-understanding paragraph); `10-philosophy.md` (the philosophy, Principle i, Principle ii, including the Socratic-versus-expository rule); `20-tools.md` (what each tool is for, carrying the `{{WEB_TOOLS}}` and `{{TOOL_PREFIX}}` placeholder tokens); `30-quiz-options.md` (already written in task 1); `40-discipline.md` (the Math Academy discipline); `50-checks.md` (what a check tells you: the confidence cases and what locking schedules); `60-process.md` (warm-up, probe, plan, teach, cumulative quiz, closing); `70-material.md` (course material, including a repository as a course); `80-library.md` (the learner's library); `90-preferences.md` (how this learner wants to be taught: language, it is theirs not yours, what beats what when they conflict); `95-writing-style.md` (writing for the screen, and the length discipline).

The union rule is absolute and is the single most important thing in this plan: **every rule any of the three copies states must survive.** Use `prompt.ts`'s phrasing as the backbone and fold in what only the other copies carry. Specifically, these must all appear in the merged body, and they are the ones to check by name because they are the ones most likely to be lost:

From `plugin/skills/teach/SKILL.md` only — the whole "Course material" section including "The material is the syllabus, not the authority", "Never walk through the slides in order", the cite rule, the disagree-when-needed rule and the repository-as-a-course rules; the whole "The learner's library" section including lean on it, point to it, grow it, disagree, and follow the nudges; the whole "What a check tells you" section including the pretest-miss case and what locking schedules; "Phase 0: Warm-up (when there is one)" as an explicit phase; the preference rules "Language", "It is theirs, not yours" and "When they conflict"; the "Accuracy is non-negotiable" paragraph; and the length paragraph ending "If you notice you have called `quiz` twice in a row with only a sentence between, stop and teach."

From `plugin/commands/learn.md` only — "A bare sequence of quizzes with one-line remarks between them is a failed lesson." and "Never end your turn in the teach phase without a card pending: write the teaching and call `quiz` in the same turn." Both belong in `95-writing-style.md` or `60-process.md`; put each where it reads naturally and keep the sentence intact.

From `server/src/prompt.ts` only — every bullet of the discipline section, in particular "Never assume a prerequisite", "Mastery before advancing", "Intuition over procedure, always", "Small bites, explicit instruction", "Layer immediately", "Block while learning, mix while checking", "Targeted remediation, not re-explanation", "Retrieval, not recognition", "Review before new work, on the forgetting curve", "The cumulative quiz" and "Do not let them be lazy"; and the "Writing style" rules.

Do not compress, shorten, merge or reword any rule to save tokens. D-03 rejects that explicitly: this is a refactor phase, compressing risks losing a rule, and Phase 4's compaction work targets tool descriptions, never the gate text. Where two copies say the same thing in different words, keep `prompt.ts`'s wording and keep any distinct clause the other copy adds. Where they genuinely conflict — the same situation, two incompatible instructions — keep both readings if both are true. If they cannot both be true, that is not a wording choice the executor makes: stop and ask the developer which rule wins, naming both copies and the conflict. D-03 settles *phrasing* in `prompt.ts`'s favour; it does not authorise dropping a rule the other copy states.

Keep the shared body surface-neutral: no sentence in `method/*.md` may name a surface, a tool prefix, the browser, the terminal or a lifecycle call. Anything that would need one goes through a placeholder token or into the surface preambles of task 3. Use no conditional blocks of any kind inside the body — placeholder substitution only, in the style of the existing `{{WEB_TOOLS}}`.

Extend `scripts/render-method.mjs` to emit every section into the generated span of every target, in filename order, and rewrite `server/src/prompt.ts`'s `PROMPT` to be assembled from `METHOD_BODY` plus the app surface preamble rather than from the inline text, with `systemPrompt(backend)` still doing the `{{WEB_TOOLS}}` substitution it does today and the four turn-prompt functions untouched.
  </action>
  <verify>
    <automated>pnpm method:check</automated>
    <fails_when>non-zero exit, or the output names any file as differing</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>pnpm build &amp;&amp; pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The rule inventory is complete with **zero omissions**. Extract every heading and every bolded bullet lead from `git show $PRE:server/src/prompt.ts`, `git show $PRE:plugin/skills/teach/SKILL.md` and `git show $PRE:plugin/commands/learn.md` — resolving `$PRE` to the literal reference commit named at the top of this plan, and confirming its four line counts first, because waves 1 and 2 and this plan's own task 1 have all committed by now and a relative `HEAD~N` would compare against a partly-migrated copy and pass vacuously. Confirm every extracted item appears in the concatenation of `method/*.md`. **Any item missing fails this task.** There is no passing path that records an omission: D-03 is absolute ("Every rule any copy states is kept — nothing in the method is lost") and explicitly rejects compression as a method change. A summary note is not a remedy and does not convert a miss into a pass.
    - If the executor concludes an omission is genuinely unavoidable (a rule that is incoherent once merged, or one that contradicts another copy irreconcilably), it **stops and asks the developer before continuing** — a blocking question naming the rule, both source copies and why it cannot be carried. It does not decide this itself, does not proceed past it, and does not record it and move on. Resolving a D-03 conflict is the developer's call, not the executor's.
    - Each of these exact strings appears in the concatenation of `method/*.md`: "The material is the syllabus, not the authority", "Never walk through the slides in order", "A bare sequence of quizzes with one-line remarks between them is a failed lesson", "Never end your turn in the teach phase without a card pending", "Do not let them be lazy", "Block while learning, mix while checking", "Targeted remediation", "Review before new work, on the forgetting curve", "stop and teach".
    - The surface-neutrality gate passes: `scripts/render-method.mjs` holds a `SURFACE_TOKENS` constant listing the tokens that may appear only in a preamble (the plugin and `claude mcp add` tool prefixes, the where-cards-are-answered field, and the lesson lifecycle calls), and the render exits non-zero naming the section file when the shared body matches any of them. Verify by pasting one such token into a `method/*.md` section and confirming `pnpm method` exits non-zero, then reverting.
    - The concatenation of `method/*.md` contains no Handlebars-style conditional opener (check with `grep -c "{{#" method/*.md | grep -v ":0" | wc -l` returning 0).
    - `grep -o "{{[A-Z_]*}}" method/*.md | sort -u` lists only `{{WEB_TOOLS}}` and `{{TOOL_PREFIX}}`.
    - The rendered app system prompt is longer than the pre-plan `PROMPT` (D-03 accepts the longer prompt as the cost): compare character counts and record both in the plan summary.
    - `pnpm method:check` exits 0 and `pnpm build && pnpm test` passes with 0 failures.
  </acceptance_criteria>
  <done>The whole teaching method is written once, as the union of the three copies, with every rule accounted for and nothing compressed.</done>
</task>

<task type="auto">
  <name>Task 3: per-surface preambles, the two Codex skill bodies, and generated allowed-tools</name>
  <read_first>
    - method/surfaces/ (as left by tasks 1 and 2) and scripts/render-method.mjs
    - plugin/skills/teach/SKILL.md (its frontmatter and the tool-mapping block at the top, which is per-surface text)
    - plugin/commands/learn.md and plugin/commands/review.md (the two allowed-tools lines and the hand-written prose bodies)
    - server/test/wire-surface.json (the ordered tool-name list, mcp surface)
    - codex/skills/derive-learn/agents/openai.yaml and codex/skills/derive-review/agents/openai.yaml (the manifests the SKILL.md bodies sit beside)
    - server/src/prompt.ts (systemPrompt and the {{WEB_TOOLS}} substitution the app preamble feeds)
  </read_first>
  <files>method/surfaces/app.md, method/surfaces/claude-code.md, method/surfaces/codex-learn.md, method/surfaces/codex-review.md, scripts/render-method.mjs, plugin/skills/teach/SKILL.md, plugin/commands/learn.md, plugin/commands/review.md, codex/skills/derive-learn/SKILL.md, codex/skills/derive-review/SKILL.md</files>
  <action>
Write the four short per-surface preambles of D-02 and finish the three generated documents.

`method/surfaces/app.md` carries only what is true of the in-app tutor: tools are called by their bare names, cards are rendered and answered in the browser, and there is no explicit lesson lifecycle because the server owns it. `method/surfaces/claude-code.md` carries the plugin's tool-mapping block (the `mcp__plugin_derive_derive__` prefix when installed as a plugin and `mcp__derive__` when added with `claude mcp add`), the browser-versus-terminal answering rules including the one-card-at-a-time rule and the `answer` round trip, and the `start_lesson` / `end_lesson` lifecycle. `method/surfaces/codex-learn.md` and `method/surfaces/codex-review.md` carry the same three things in Codex's terms, plus the review session's own framing for the review skill. Keep each preamble short: per D-02 drift can only occur here, and a diff must show at a glance that the body below is identical everywhere.

Define the placeholder values per surface in the render script: `{{WEB_TOOLS}}` resolves to the backtick-quoted `WebSearch` / `WebFetch` pair for the app and the Claude Code surfaces and to Codex's built-in web search for the Codex surfaces — matching exactly what `systemPrompt(backend)` substitutes today; `{{TOOL_PREFIX}}` resolves to the empty string for the app surface and to each terminal surface's MCP prefix.

Render the three whole-file targets: `plugin/skills/teach/SKILL.md` becomes its hand-written frontmatter plus the `claude-code` preamble plus the shared body; `codex/skills/derive-learn/SKILL.md` and `codex/skills/derive-review/SKILL.md` become their frontmatter plus their preamble plus the shared body. FOUND-02 names the Codex skills and today they have no body at all, so this closes a real method gap rather than moving text around.

Generate the `allowed-tools:` line of `plugin/commands/learn.md` and `plugin/commands/review.md` per D-07. In the render script, declare each command's tool set as a projection of the registry — every tool with `mcp` among its surfaces (22 of them, per plan 01-01), minus a per-command exclusion list whose every entry carries a one-line reason — then prefix each name with `mcp__plugin_derive_derive__` and append the fixed tail `WebSearch, WebFetch, Skill`, which are not registry tools. Seed the exclusion lists so that the rendered lines are byte-identical to what is committed today. **This is where the phase's two counts meet and must not be confused: the registry holds 22 `mcp`-surface tools, and `plugin/commands/learn.md`'s `allowed-tools` line holds 21 of them.** The difference is exactly one entry: the `library` catalog tool is excluded from both commands (the plugin commands have never allowed it, and allowing it now would widen what the plugin may call — a capability change, not a refactor). Do not resolve the gap the other way by dropping `library` from the registry; it is on the MCP wire and the snapshot must cover it (D-08). `review.md` additionally excludes the material and resource-ingestion tools it has never allowed, each with its reason, leaving its 15. A name in an exclusion list that is not in the registry is a hard render failure, so a renamed or removed tool cannot leave a stale exclusion behind. The prose body of both command files stays hand-written and is not touched by the render.

Thin `plugin/commands/learn.md`'s prose body to a thin invocation, as CONTEXT.md's open items say: steps 4 to 6 are now in the method body, so the command keeps loading the `teach` skill, calling `start_lesson` with its argument parsing, and the "if the server is not running" fallback, and drops the teaching rules it used to restate. Confirm before deleting each sentence that its content is present in `method/*.md`; anything not yet there goes into the method body first.
  </action>
  <verify>
    <automated>pnpm method:check</automated>
    <fails_when>non-zero exit, or the output names any file as differing</fails_when>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>pnpm build &amp;&amp; pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0</fails_when>
    <automated>git diff --stat -- plugin/commands/learn.md plugin/commands/review.md</automated>
    <fails_when>the diff touches the allowed-tools line of either file with a changed tool name — compare the rendered line against `git show $PRE:plugin/commands/learn.md` and confirm the set of tool names is unchanged</fails_when>
  </verify>
  <acceptance_criteria>
    - The `allowed-tools:` line rendered into `plugin/commands/learn.md` names exactly the same 21 prefixed tools plus `WebSearch, WebFetch, Skill` as the line at `git show $PRE:plugin/commands/learn.md`, as an unordered set comparison. 21, not 22: this is the command's allow-list, and `library` is the one registry tool it excludes. Assert both numbers in the same check — `toolsFor('mcp')` has 22 entries and the rendered line has 21 prefixed names — so the two can never be conflated.
    - The `allowed-tools:` line rendered into `plugin/commands/review.md` names exactly the same 15 prefixed tools plus the fixed tail as the line at `git show $PRE:plugin/commands/review.md`.
    - Removing a tool from `TOOL_REGISTRY` and running `pnpm method` exits non-zero naming the stale exclusion or the missing tool (verify once by hand, then revert).
    - The shared body text is byte-identical across `plugin/skills/teach/SKILL.md`, `codex/skills/derive-learn/SKILL.md` and `codex/skills/derive-review/SKILL.md`: extract the region below each file's preamble and confirm all three are equal.
    - Each of the four files under `method/surfaces/` is under 60 lines.
    - Every sentence removed from `plugin/commands/learn.md`'s prose body has a counterpart in `method/*.md`; list the mapping in the plan summary.
    - `pnpm method:check` exits 0 and `pnpm build && pnpm test` passes with 0 failures.
  </acceptance_criteria>
  <done>Four short preambles carry every per-surface difference, the two Codex skills have real method bodies for the first time, and the plugin commands' allowed-tools lists are generated from the registry with the same drift gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> model instructions | `method/*.md` becomes the text every provider's model is given; anything written there is executed as instructions by an LLM |
| render script -> committed files | `scripts/render-method.mjs` writes into `plugin/`, `codex/` and `server/src/` |
| CI -> repo | the drift check reads the tree; it must never write to it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | `method/*.md` as model instructions | high | mitigate | every rendered copy is committed and `pnpm method:check` fails CI on drift (D-04), so a change to what any model is told appears as a reviewable diff in the pull request and cannot land silently |
| T-03-02 | Tampering | `scripts/render-method.mjs` write targets | medium | mitigate | the script writes only to an explicit, enumerated list of target paths under the repo root, resolved from `import.meta.url`; it takes no path argument and writes nothing derived from file content |
| T-03-03 | Repudiation | CI auto-regenerating the copies | medium | mitigate | D-04 is explicit that CI fails rather than regenerating and pushing; the CI step runs `check`, never `render`, and the repository token is not used to write |
| T-03-04 | Information disclosure | the render temp directory | low | mitigate | `scripts/check-method.mjs` uses `mkdtempSync` under the system temp dir and removes it; it writes no repository content anywhere else |
| T-03-05 | Elevation of privilege | generated `allowed-tools` widening the plugin's capability | high | mitigate | the rendered lists are asserted byte-equivalent to the committed ones by set comparison, exclusions carry reasons, and an unknown exclusion name is a hard render failure — a tool cannot be silently added to what the plugin may call |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no dependency; the scripts run on bare Node with only `node:` imports. If execution finds it needs a package, stop and raise it rather than installing |
</threat_model>

<verification>
- `pnpm method:check` exits 0 on a clean tree and non-zero after a one-character edit to any rendered copy.
- `pnpm typecheck` clean; `pnpm build && pnpm test` green.
- The rule inventory of task 2 shows **no omission at all** from any of the three source copies, compared against the literal pre-phase commit `PRE`; an omission fails the task rather than being recorded and passed.
- The two `allowed-tools` lines name the same tool sets as before the plan.
- `node --check` passes on both new scripts.
</verification>

<success_criteria>
- The method is written once under `method/` and rendered to the app system prompt, the plugin skill, both Codex skills and the two `allowed-tools` lists (D-01, D-07).
- One surface-neutral body is shared verbatim; only four short preambles differ (D-02).
- The body is the union of the three existing copies with nothing compressed or lost (D-03).
- CI fails when any rendered copy drifts (D-04).
- The two counts stay distinct and both hold: 22 tools on the `mcp` surface of the registry, 21 prefixed names in `learn.md`'s `allowed-tools` (the excluded one is `library`), 15 in `review.md`'s.
- The Codex skills have real method bodies for the first time.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
