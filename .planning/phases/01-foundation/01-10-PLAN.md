---
phase: 01-foundation
plan: 10
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - server/src/repo.ts
  - server/test/guards.test.ts
autonomous: true
requirements: [FOUND-06]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every egress runs the same destination guard: assertPublicHost is called inside fromGitClone before mkdtempSync and before the git process is spawned, so grep -c assertPublicHost server/src/repo.ts is at least 1 and the call precedes the scratch directory."
    - "collectRepo('https://127.0.0.1/x.git') rejects with the guard's own private-or-local message and leaves no clone- scratch directory in the data directory."
    - "collectRepo('https://192.168.0.5/internal.git') and collectRepo of a link-local, CGNAT or IPv6 loopback git URL reject the same way, because the guard judges resolved addresses rather than names."
    - "A hostname that resolves to a private address is refused as well as a literal private address, since assertPublicHost resolves before it judges."
    - "The existing https-only scheme check still runs first, so an ssh, git or git@ URL still fails with the sentence about the learner's own keys rather than with a lookup error."
    - "A public git URL is unaffected: the guard returns without throwing and the clone proceeds exactly as before."
    - "server/test/guards.test.ts's doc comment states only what its cases exercise, and its cloning suite now drives collectRepo at a private address rather than testing the scheme alone."
    - "Importing server/src/repo.ts still resolves at runtime after it begins importing server/src/library.ts, despite the library -> materials -> repo import cycle."
  artifacts:
    - server/src/repo.ts
    - server/test/guards.test.ts
  key_links:
    - "the MCP attach_material tool -> POST /api/materials/repo -> materials.ts -> collectRepo -> fromGitClone -> assertPublicHost"
    - "library.ts assertPublicHost -> repo.ts fromGitClone (the link the verification recorded as NOT WIRED)"
    - "assertPublicHost -> the refusal, before mkdtempSync -> no clone- scratch directory is ever created"
  prohibitions:
    - "Never make a test pass by narrowing what it asserts; guards.test.ts already shipped green while asserting that neither egress could be pointed at this machine and testing only the git scheme."
    - "Never send learner data or any credential off this machine; no gap fix may introduce a call to a service the author runs."
---

<objective>
Close the one egress that never ran the guard.

Every fetch this phase added refuses loopback, link-local, RFC-1918 and CGNAT
destinations and re-checks on every redirect hop — except `git clone`.
`assertPublicHost` appears zero times in `server/src/repo.ts`, and `collectRepo`
routes every non-GitHub repo URL to `fromGitClone`. The MCP `attach_material`
tool takes that URL straight from the model, so this is exactly the
model-steered path D-10's guard exists for: `https://127.0.0.1/x.git` and
`https://192.168.0.5/internal.git` clone from internal hosts today and become
readable course material.

The test suite is green over it in a specific way worth naming: `guards.test.ts`
opens by asserting neither egress "can be pointed at this machine" and then only
tests the git *scheme*. The fix and the missing case land together.

Purpose: the third gap of Success Criterion 5 — every egress running the same
guard (FOUND-06, D-10).
Output: one awaited call before the scratch directory, and a case that drives
`collectRepo` at a private address and checks the data directory afterwards.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Created by **this plan**: no new file and no new exported symbol. `fromGitClone`
in `server/src/repo.ts` changes from a synchronous function to an `async` one and
gains an import of `assertPublicHost` from `./library.js`; `collectRepo` awaits
it. `server/test/guards.test.ts` gains cases inside its existing
`describe('cloning')` block and reuses the `cloneDirs()` helper that is already
there.

<tasks>

<task type="tracer">
  <name>Task 1: a private git URL is refused at the entry point the model actually uses</name>
  <reversibility rating="reversible">One awaited call and an `async` keyword; reverting is deleting both. No stored state, no schema, no wire contract.</reversibility>
  <read_first>
    - `server/src/repo.ts` lines 1-20 (the module doc block and the imports, currently `./config.js` only) and lines 265-300 in full: `fromGitClone` with its https scheme check, `mkdtempSync`, the `execFileSync` git spawn, both `rmSync` cleanups, and `collectRepo` with its three-way dispatch
    - `server/src/library.ts` lines 145-175: `assertPublicHost` — that it is `async`, that it resolves a name to every address before judging, and the exact wording of its two throws
    - `server/src/library.ts` line 31 and lines 380-450: proof that `partsOf`, `SEP` and `titleOf` are used only inside function bodies, never at module evaluation time — this is what makes the `repo -> library -> materials -> repo` import cycle safe
    - `server/src/materials.ts` line 16 and line 183: `collectRepo` is imported at the top and called only inside a function, the other half of the same argument
    - `server/test/guards.test.ts` lines 1-30 (the doc comment that overstates, the `DERIVE_DATA_DIR` scratch setup before the dynamic imports, and the `cloneDirs()` helper) and lines 124-133 (the whole `describe('cloning')` block)
    - `.planning/phases/01-foundation/01-VERIFICATION.md` lines 117-124 (the `repo.ts` issue) and line 241 (the key link recorded as NOT WIRED)
    - `.planning/phases/01-foundation/01-08-PLAN.md` — the plan that authored `assertPublicHost` and the repo guards, for D-10's reasoning
  </read_first>
  <files>server/src/repo.ts, server/test/guards.test.ts</files>
  <action>
Run the guard on the path that never ran it, at the entry point the model reaches.

In `server/src/repo.ts`, import `assertPublicHost` from `./library.js` — server imports carry the `.js` extension under NodeNext. Make `fromGitClone` `async` and give it the return type `Promise<RepoSource>`. Immediately after the existing https scheme check and before `mkdtempSync`, `await assertPublicHost(url.trim())`. Order matters in both directions: the scheme check stays first so an ssh, git or `git@` URL still fails with the existing sentence about the learner's own keys rather than with a URL-parse or lookup error, and the host guard runs before the scratch directory exists so a refusal leaves nothing behind for anyone to clean up. Extend the existing one-line comment above the scheme check so it covers both reasons the check is before the spawn — the learner's keys, and the destination.

In `collectRepo`, change the `fromGitClone` branch to `return await fromGitClone(s)`. It is already `async`, so nothing else moves.

Be deliberate about the import cycle this creates: `repo.ts` now imports `library.ts`, which imports `materials.ts`, which imports `repo.ts`. It resolves because no module in the ring touches another's bindings at module-evaluation time — `library.ts` uses `partsOf`, `SEP` and `titleOf` only inside function bodies, and `materials.ts` calls `collectRepo` only inside `attachRepo`. Do not add a module-level use of any of those names while in these files, and do not "fix" the cycle by copying `assertPublicHost`: a second copy of a destination guard is how the two drift apart. One consequence to expect: importing `repo.ts` now transitively reaches `db.ts`, which opens SQLite at import time, so any consumer that imports it must already have `DERIVE_DATA_DIR` set — `server/test/guards.test.ts` does this on line 22 and its comment already says so.

Then the case that proves it, in the same commit. Rewrite the `server/test/guards.test.ts` doc comment so it claims only what the cases exercise. In the existing `describe('cloning')` block, keep the scheme case as it is and add: `collectRepo('https://127.0.0.1/x.git')` rejects with a message matching the guard's private-or-local wording, and `cloneDirs()` is empty afterwards, which is the assertion that the refusal happened before `mkdtempSync` and therefore before git; the same for `https://192.168.0.5/internal.git`, `https://169.254.169.254/x.git` and `https://[::1]/x.git`, each checked the same way. Assert `cloneDirs()` is empty once at the end of the block as well, so a directory left by any one of them fails the case rather than being masked by the next.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>cd server &amp;&amp; node --import tsx --test test/guards.test.ts</automated>
    <fails_when>non-zero exit, or any line beginning "not ok" in the TAP output, or the summary reports "# pass 0"</fails_when>
    <automated>cd server &amp;&amp; DERIVE_DATA_DIR=$(mktemp -d) node --import tsx -e "import('./src/repo.ts').then((m) =&gt; console.log(typeof m.collectRepo))"</automated>
    <fails_when>the output is anything other than the single word "function", or stderr contains "Cannot access" or "before initialization", or non-zero exit — any of which means the new import cycle does not resolve</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c assertPublicHost server/src/repo.ts` prints at least `1` (it printed `0` at verification time).
    - In `server/src/repo.ts` the line number of the `assertPublicHost` call is lower than the line number of `mkdtempSync` inside `fromGitClone`: `grep -n 'assertPublicHost\|mkdtempSync' server/src/repo.ts` shows the guard first within that function.
    - `cd server && node --import tsx --test test/guards.test.ts` exits 0 with no line beginning `not ok`, and its output includes a cloning case naming a private address.
    - A direct drive proves the refusal and the absence of a scratch directory: with `DERIVE_DATA_DIR` set to an empty temp directory, `collectRepo('https://127.0.0.1/x.git')` rejects with a message containing `private or local address`, and `ls $DERIVE_DATA_DIR` lists no entry beginning `clone-`.
    - `collectRepo('ssh://git@github.com/jicanta/derive.git')` still rejects with a message containing `only clones over https` — the scheme check still runs first.
    - The import-cycle smoke above prints `function`.
    - `git diff --quiet -- web/` exits 0.
  </acceptance_criteria>
  <done>A git URL pointing at this machine or at any private range is refused by the same guard every other egress uses, before a directory is made or a process is spawned, and the suite that claimed this now demonstrates it.</done>
</task>

<task type="auto">
  <name>Task 2: the whole suite stays green with repo.ts on the new import ring</name>
  <read_first>
    - `server/test/guards.test.ts` as left by task 1
    - `server/test/api.test.ts` lines 1-70: the spawn harness that drives the built server, which is the consumer that exercises `materials.ts` and therefore the cycle in production shape
    - `server/src/materials.ts` lines 175-200: `attachRepo`, the one call site of `collectRepo`
    - `server/src/index.ts` the `/api/materials/repo` route handler and its `err()` usage, to confirm an async rejection from `collectRepo` still lands as the existing 4xx `{ error }` body rather than an unhandled rejection
  </read_first>
  <files>server/test/guards.test.ts</files>
  <action>
Prove the change costs nothing anywhere else, and that the route still reports the refusal as the method intends.

Run the whole model-free gate and read its output rather than only its exit code: `pnpm build` then `pnpm test`. The suites that matter here are `api.test.ts`, which spawns the built server and drives a whole lesson through routes that reach `materials.ts` and therefore the new import ring, and `mcp.test.ts`, which drives `server/dist/mcp.js` over stdio — the MCP `attach_material` tool is the caller that hands `collectRepo` a model-supplied URL, so a break in the ring would surface there first.

Confirm the refusal reaches the caller as a message, not as a crash. `POST /api/materials/repo` with a body whose `source` is `https://127.0.0.1/x.git` must return a 4xx `{ error }` body carrying the guard's sentence, the same shape a scheme refusal returns today; a rejected promise escaping into an unhandled rejection would show as a 500 or as a console trace instead. `collectRepo` was already async before this plan and `materials.ts` already awaits it, so this is an observation, not an edit: **do not modify `server/src/index.ts` or `server/src/materials.ts` in this plan.** Plan 01-09 owns `server/src/index.ts` in this same wave, so an edit here would collide. If the observation comes out wrong, stop and report it in the summary rather than fixing it here.

Add one case to `server/test/guards.test.ts` that pins the behaviour the module-level comment now claims: a hostname form, not just a literal address. Drive `collectRepo` at a URL whose host is a name that resolves to loopback on every machine — use `localhost` — and assert it rejects with the private-or-local message and leaves `cloneDirs()` empty. That is the case that proves the guard resolves before it judges, which is the difference between this guard and a string blocklist.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0, or "# pass" reports fewer tests than the 121 this phase already had</fails_when>
    <automated>cd server &amp;&amp; node --import tsx --test test/guards.test.ts test/api.test.ts</automated>
    <fails_when>non-zero exit, or any line beginning "not ok" in the TAP output</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm test` reports `# fail 0` and at least the 121 tests the phase had before this plan.
    - `cd server && node --import tsx --test test/guards.test.ts` includes a passing case that drives `collectRepo` at a `localhost` git URL and asserts the private-or-local message.
    - Against a running scratch server with a valid credential, `POST /api/materials/repo` with `{"source":"https://127.0.0.1/x.git"}` returns a 4xx status and a JSON body whose `error` contains `private or local address`; the server's stdout and stderr carry no unhandled-rejection trace.
    - `cloneDirs()` is asserted empty in every cloning case, and `ls "$DERIVE_DATA_DIR"` after the suite lists no entry beginning `clone-`.
    - `git diff --quiet -- web/` exits 0.
  </acceptance_criteria>
  <done>The new import ring costs nothing: the full suite, the built server and the stdio MCP path are all green, and a model-supplied private git URL comes back to the caller as the guard's sentence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-supplied URL -> `git clone` | the MCP `attach_material` tool takes a repo URL straight from the model and `collectRepo` routes every non-GitHub URL to `fromGitClone` |
| cloned repository -> course material | whatever the clone contains becomes text the model can read through `read_material` and `search_material` |
| the test suite -> the guarantees it states | `guards.test.ts` asserted that neither egress could be pointed at this machine while testing only the git scheme |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information disclosure | SSRF to loopback, RFC-1918, link-local or CGNAT through `fromGitClone` (`server/src/repo.ts:271`) | high | mitigate | `await assertPublicHost(url)` before `mkdtempSync` and before the git spawn, so the clone path runs the same resolved-address guard every other egress runs (D-10) |
| T-10-02 | Information disclosure | the cloud metadata endpoint (`169.254.169.254`) reached as a git host | high | mitigate | covered by the same guard, which already refuses link-local including the metadata address; asserted as its own case |
| T-10-03 | Spoofing | a hostname that resolves to a private address slipping past a name-based check | high | mitigate | `assertPublicHost` resolves the name and judges every answered address; a `localhost` case pins that this is resolution and not a string blocklist |
| T-10-04 | Elevation of privilege | `git clone` over ssh using the learner's own keys | high | mitigate | unchanged and still first: the https-only scheme check runs before the host guard, keeping its existing sentence |
| T-10-05 | Repudiation | a suite green over a guarantee it does not exercise | high | mitigate | the doc comment is rewritten to match the cases, and each new case asserts both the rejection and that no `clone-` scratch directory was left behind |
| T-10-06 | Spoofing | DNS rebinding between `assertPublicHost`'s lookup and git's own resolution | medium | accept | a time-of-check-to-time-of-use window remains, as it does for any userland guard in Node and as plan 01-08 already recorded for the library fetch; closing it needs a socket-level lookup git does not expose |
| T-10-07 | Denial of service | a clone that never finishes | low | accept | the existing 120-second `execFileSync` timeout is unchanged and already bounds it |
| T-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no dependency; `assertPublicHost` already exists in the tree and uses only `node:dns/promises` and `node:net`. If execution finds it needs a package, stop and raise it rather than installing |
</threat_model>

<verification>
- `grep -c assertPublicHost server/src/repo.ts` is at least 1, and the call sits above `mkdtempSync` in `fromGitClone`.
- `cd server && node --import tsx --test test/guards.test.ts` green, with cases for a literal loopback address, an RFC-1918 address, the metadata address, IPv6 loopback and a resolving hostname.
- No `clone-` directory remains in the scratch data directory after the suite.
- `pnpm build && pnpm test` at 0 failures, including `api.test.ts` and `mcp.test.ts`, which is the proof the new import ring resolves in the built server and in the stdio MCP process.
- `git diff --quiet -- web/` exits 0 for the whole plan.

</verification>

<success_criteria>
- Every egress runs the same destination guard; the key link `library.ts assertPublicHost -> repo.ts fromGitClone` is wired (FOUND-06, D-10).
- A model-supplied private git URL is refused before a directory is made or a process is spawned, and the refusal reaches the caller as the guard's own sentence.
- `guards.test.ts` demonstrates the guarantee its doc comment states.

</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-10-SUMMARY.md` when done
</output>
