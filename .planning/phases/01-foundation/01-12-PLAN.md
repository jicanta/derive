---
phase: 01-foundation
plan: 12
type: execute
wave: 4
depends_on: ["01-09", "01-10", "01-11"]
gap_closure: true
files_modified: []
autonomous: true
requirements: [FOUND-04, FOUND-06]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every behavioural spot-check the verification recorded as FAIL is re-run with the same instrument the verifier used — raw curl against the built server, not the test suite — and reproduces as PASS on the rebuilt tree."
    - "The three token-disclosure reproductions pass: / with no headers carries no token, the catch-all carries no token, and / with Host: evil.com and Origin: http://evil.com is refused rather than answered."
    - "The two widened-bind reproductions pass: a correct-token request naming the address it arrived on is admitted, and a forged loopback Host from another address is refused."
    - "The clone reproduction passes: collectRepo at a loopback git URL rejects and leaves no clone- scratch directory."
    - "The deletion reproduction passes: a learner removed after a turn and a usage row leaves zero rows in either table."
    - "The machine half of FOUND-04 is still green after the authentication change: the wire-surface fixture is unchanged, the stdio MCP smoke test drives server/dist/mcp.js through a whole lesson, and the plugin hook's header path still authenticates."
    - "The phase's cross-cutting constraint holds across all four gap-closure plans: git diff --quiet -- web/ exits 0."
    - "Two tools that share a name or a description across surfaces are shown as such by the wire-surface fixture rather than merged: pnpm --filter server exec node --import tsx --test test/wire-surface.test.ts exits 0 with the fixture's 22 MCP, 14 agent and 16 HTTP rows intact."
    - "Tool descriptions are compared byte-for-byte against the committed fixture rather than normalized: the wire-surface test exits 0 and git diff --quiet -- server/test/wire-surface.json exits 0."
    - "tools/list emits registry declaration order and the committed fixture asserts that order; the wire-surface test exits 0."
    - "A missing method/ section fails the render loudly rather than rendering an empty target: node scripts/check-method.mjs exits 0 on the unmodified tree and names the missing section when one is removed."
    - "check-method.mjs compares every rendered copy byte-for-byte; it exits 0 on the unmodified tree and prints that 6 rendered copies match method/."
    - "A driver that reports nothing still ends its turn exactly once: pnpm --filter server exec node --import tsx --test test/driver.test.ts exits 0."
    - statement: "A surface with no declared tools renders an empty allowed-tools list rather than omitting the line; no such surface exists in the registry today."
      verification: backstop
    - statement: "Two drivers reporting the same event for the same turn produce two sink calls and two events, never one merged event."
      verification: backstop
    - statement: "Events emitted through the sink keep their emission order in the stored sequence and in the SSE replay."
      verification: backstop
  artifacts:
    - .planning/phases/01-foundation/01-12-SUMMARY.md
  key_links:
    - "the rebuilt server/dist/index.js -> the verifier's own curl reproductions -> the evidence a re-verification pass needs"
    - "DATA_DIR/token -> server/dist/mcp.js and plugin/hooks/mirror.mjs -> the unchanged x-derive-token header path (FOUND-04's machine half)"
    - "the browser companion page -> the derive_session cookie -> the SSE stream and every card answer (the path the human check walks)"
  prohibitions:
    - "Never make a check pass by narrowing what it asserts; this plan exists because three green suites stated guarantees their cases did not exercise."
    - "Never delete or migrate a learner's real ~/.derive data while running these reproductions: every command here uses a scratch DERIVE_DATA_DIR, and the human run is the only one that touches the learner's own database."
    - "Never send learner data, the install token or any derived credential off this machine; no check here may introduce a call to a service the author runs."
---

<objective>
Prove the gaps closed with the instrument that found them, then hand the phase
its one remaining human question.

The verification's behavioural spot-check table is the artifact this phase failed
against: `curl http://127.0.0.1:PORT/` returning a token, a forged
`Host: 127.0.0.1` admitted from a LAN address, `assertPublicHost` absent from
the clone path. Plans 01-09, 01-10 and 01-11 close those in code and in the test
suite. This plan re-runs the verifier's own commands — raw `curl` against the
built server, a direct drive of `collectRepo`, a row count on a scratch database —
because a suite that passes and a reproduction that passes are two different
facts, and this phase has already demonstrated the gap between them.

Then the one item that was never failed, only unproven: Success Criterion 1's
human half. One real lesson through the Claude Code plugin and one through the
Codex skills, which `01-08-SUMMARY.md:183` records as not performed. It runs
here and not earlier for a reason the verification states: step 1 opens the
companion page in a browser, and that is the exact path the token disclosure sat
on. It is also the path the new session cookie changes, so a short browser smoke
runs first.

Purpose: FOUND-04's human half, and the evidence a re-verification pass will
look for on FOUND-06.
Output: no source change — a recorded reproduction of every failed check and two
human checks queued for the phase UAT.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Created by **this plan**: no source file, no symbol, no test. Its only output is
`.planning/phases/01-foundation/01-12-SUMMARY.md`, carrying the reproduction
table and the two `<human-check>` scripts for end-of-phase harvest.

## Flagged Assumptions (unresolved edge probes)

The spec-less edge probe returned two rows the classifier could not categorise.
Neither is auto-resolved; both are surfaced here as explicit assumptions for a
human to settle:

- **FOUND-04 — `unclassified — review manually`.** Assumption: "keep working"
  means a learner reports no behavioural difference between a lesson run before
  this phase and one run after it, judged by the three-step script on task 2.
  There is no machine definition of it, which is why it is a `<human-check>` and
  why plan 01-08 could not discharge it.
- **FOUND-05 — `unclassified — review manually`.** Assumption: "migrates
  forward" was settled behaviourally at verification time by the real
  `~/.derive/derive.db` reaching `user_version 3` with `turns` and `usage`
  present, and plan 01-11 adds no migration, so nothing in this gap closure
  changes it. Recorded rather than re-verified.

<tasks>

<task type="auto">
  <name>Task 1: re-run the verifier's own failed checks against the rebuilt tree</name>
  <precondition>Plans 01-09, 01-10 and 01-11 are merged and `pnpm build` has produced `server/dist/index.js`, `server/dist/mcp.js` and `web/dist/index.html` — every reproduction below drives the built server rather than the source.</precondition>
  <read_first>
    - `.planning/phases/01-foundation/01-VERIFICATION.md` lines 253-279 in full: the behavioural spot-check table, including the eight rows marked ✗ FAIL and the exact command in each row's Command column
    - `.planning/phases/01-foundation/01-VERIFICATION.md` lines 351-411: the Gaps Summary, for what each reproduction is meant to demonstrate
    - `.planning/phases/01-foundation/01-09-SUMMARY.md`, `01-10-SUMMARY.md` and `01-11-SUMMARY.md` as written by this wave's predecessors: what actually landed, and the name and shape of the session cookie
    - `server/test/security.test.ts` and `server/test/guards.test.ts` as left by 01-09 and 01-10, so the reproductions here check the same properties from outside rather than repeating the same assertions
  </read_first>
  <files>none — this task runs commands and records their output in the plan summary</files>
  <action>
Reproduce each failed row, with the instrument that found it, and record the result.

Start one server on a scratch data directory and a free high port: `DERIVE_DATA_DIR=$(mktemp -d) PORT=$P node server/dist/index.js`. Keep the token path to hand as `$DERIVE_DATA_DIR/token`. Every check below is a `curl` against that server, matching the verifier's method rather than the suite's.

The three token-disclosure rows. `GET /` with no headers: the body must carry the app markup and no token, and no response header may carry the token either. The catch-all, `GET /anything/at/all`: the same. `GET /` carrying `Host: evil.com` and `Origin: http://evil.com`: refused with 403 rather than answered with 200. Then the fourth row, the consequence: there is no value to harvest off `/` that unlocks `/api/lessons`, while a request carrying the session cookie the document response did set returns 200 and a request carrying the `x-derive-token` header from the file returns 200.

The three widened-bind rows. Start a second server with `DERIVE_HOST=0.0.0.0` on its own port and its own scratch directory. Over loopback, a correct-token request naming `127.0.0.1:<its port>` is admitted; a correct-token request naming a private address the connection did not arrive on is refused; and `0.0.0.0:<its port>` is refused as a name. When the machine has a non-loopback address, repeat the verifier's LAN pair against it: naming that address is admitted, naming `127.0.0.1` from it is refused. Record "no non-loopback interface on this machine" rather than a pass when there is none — an unrunnable check is not a passing one, and writing it down as a pass is the failure mode this whole plan exists to avoid.

The clone row. With `DERIVE_DATA_DIR` pointed at an empty temp directory, drive `collectRepo('https://127.0.0.1/x.git')` directly through tsx and confirm it rejects with the private-or-local sentence, then list that directory and confirm no entry begins `clone-`.

The deletion row, which the verification carried as review finding WR-01 rather than as a spot-check. On a scratch database: create a learner, a lesson, a turn and a usage row; delete the learner; count `usage` and `turns` rows carrying that learner id and confirm both are zero.

Record every one of these in the plan summary as a small table with the row from the verification, the command run, and the observed result. Where a row cannot be run, say so with its reason. Do not edit any source file in this task: the three plans this one depends on own every file involved, and a fix discovered here belongs back in the plan that owns it, reported rather than patched.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or server/dist/index.js or web/dist/index.html is absent afterwards</fails_when>
    <automated>git diff --quiet -- web/</automated>
    <fails_when>non-zero exit, which means one of the four gap-closure plans modified a file under web/ and broke the phase's cross-cutting constraint</fails_when>
    <human-check>
      A two-minute browser smoke, because the gap closure changed how the browser authenticates and no automated test drives a real browser.

      With the Derive server running (`pnpm start`), open `http://localhost:4310/` in your normal browser and confirm: the Home page loads with your lessons listed rather than an error; opening a lesson shows its timeline and its graph; the page does not show a 401 or an "unauthorized" message anywhere; and if a lesson is mid-turn, text still streams in rather than stalling. Then open the browser's developer tools, Application or Storage panel, and confirm a `derive_session` cookie exists for this origin and that its value is **not** the contents of `~/.derive/token`.

      Report anything that shows an error, or any page that loads empty where it used to have content.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The plan summary contains a table with one row per failed spot-check from `01-VERIFICATION.md` lines 272-278, each with the command run and the observed result.
    - `curl -s http://127.0.0.1:$P/ | grep -c 'name="derive-token"'` prints `0` while `curl -s http://127.0.0.1:$P/ | grep -c '<div id="root"'` prints at least `1`.
    - `curl -s -D - -o /dev/null http://127.0.0.1:$P/ | grep -c "$(cat $DERIVE_DATA_DIR/token)"` prints `0`.
    - `curl -s -o /dev/null -w "%{http_code}" -H "Host: evil.com" -H "Origin: http://evil.com" http://127.0.0.1:$P/` prints `403`.
    - `curl -s -o /dev/null -w "%{http_code}" -H "x-derive-token: $(cat $DERIVE_DATA_DIR/token)" http://127.0.0.1:$P/api/lessons` prints `200`.
    - Against the `DERIVE_HOST=0.0.0.0` server, a correct-token request naming `0.0.0.0:<port>` prints `403`, and a correct-token request naming a private address the connection did not arrive on prints `403`.
    - Driving `collectRepo('https://127.0.0.1/x.git')` rejects with a message containing `private or local address`, and `ls "$DERIVE_DATA_DIR"` lists no entry beginning `clone-`.
    - After deleting a learner who had a turn and a usage row, `SELECT COUNT(*) FROM usage WHERE learner_id = ?` and the same over `turns` both return `0`.
    - `git diff --quiet -- web/` exits 0, and `git status --porcelain server/ scripts/` is empty after this task — it changed no source.
    - The `<human-check>` above is recorded for end-of-phase harvest into the phase UAT.
  </acceptance_criteria>
  <done>Every row the verification marked ✗ FAIL has been re-run with the verifier's own instrument against the rebuilt tree, and its new result is written down — including any row that could not be run, and why.</done>
</task>

<task type="auto">
  <name>Task 2: the model-free gate, then the plugin and Codex parity run</name>
  <read_first>
    - `.planning/phases/01-foundation/01-VERIFICATION.md` lines 143-167 (the `behavior_unverified_items` and `human_verification` blocks) and lines 320-349 (the human verification section with its three-step script and its sequencing note)
    - `.planning/phases/01-foundation/01-08-PLAN.md` task 3 and `01-08-SUMMARY.md` line 183: the same check as plan 01-08 carried it, and the record that it was not performed
    - `plugin/commands/learn.md` and `plugin/commands/review.md`: what `/derive:learn` and `/derive:review` invoke and which tools they allow
    - `plugin/hooks/mirror.mjs` lines 26-40: the token read and the header every hook request carries, which is the path the authentication change must not have broken
    - `codex/skills/derive-learn/SKILL.md` first 40 lines: the method body the Codex skill now has, which is what makes step 2 the first real test of it
    - `scripts/doctor.mjs` in full, since the human run starts from a green doctor
  </read_first>
  <files>none — this task runs the model-free gate and records the human script in the plan summary</files>
  <action>
Confirm the machine half of Success Criterion 1 survived the authentication change, then set up and record the human half.

Run the whole model-free gate and read the output, not only the exit codes: `pnpm typecheck`, `pnpm build`, `pnpm test`, `node scripts/check-method.mjs`, `node scripts/doctor.mjs`, and `git diff --quiet -- server/test/wire-surface.json`. The suites that carry FOUND-04's machine half are `wire-surface.test.ts`, which must still match the committed fixture in all three surfaces, and `mcp.test.ts`, which drives the built `server/dist/mcp.js` over stdio through `tools/list`, `start_lesson`, `set_plan`, `quiz`, `answer` and `end_lesson`. Those two are the direct proof that the `x-derive-token` header path the plugin and the Codex skills use still authenticates after plan 01-09 changed how the browser authenticates. Record the counts — tests, suites, failures — in the plan summary, because the phase's own baseline is 121 tests and a smaller number is a suite that stopped running rather than a suite that got faster.

Then leave the machine in the state the human run needs and say so in the summary: the Derive server running with `/api/health` answering, the plugin installed from this repo, `~/.derive/token` present at mode 0600, and the repository path and the server URL written down. `node scripts/doctor.mjs` green is the one-command form of that.

The human run itself is the `<human-check>` below. It is the phase's last open question and it needs a real model and a real login on two terminals, so nothing in this task can discharge it — record it, do not simulate it. It runs after this whole gap closure rather than inside plan 01-08 for the reason the verification gives: its first step opens the companion page in a browser, which is the path the token disclosure sat on and the path the session cookie now changes.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>pnpm build</automated>
    <fails_when>non-zero exit, or the output contains "error TS", or server/dist/index.js or web/dist/index.html is absent afterwards</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0, or "# pass" reports fewer than the 121 tests this phase had before the gap closure</fails_when>
    <automated>node scripts/check-method.mjs</automated>
    <fails_when>non-zero exit, or the output does not contain "6 rendered copies match method/"</fails_when>
    <automated>git diff --quiet -- server/test/wire-surface.json</automated>
    <fails_when>non-zero exit, which means the tool wire surface changed during a gap closure that was supposed to touch neither the registry nor any tool description</fails_when>
    <automated>node scripts/doctor.mjs</automated>
    <fails_when>non-zero exit, or the output contains a FAIL line, or the Install token line does not report mode 0600</fails_when>
    <human-check>
      Run one real lesson through each terminal path and confirm both complete exactly as before. The Derive server is already running, the plugin is installed from this repo, and the token is in place; nothing needs setting up.

      1. Claude Code plugin: in a Claude Code session with the plugin installed from this repo, run `/derive:learn` on any small topic. Confirm: the companion page opens in the browser and loads without an authorization error; the tutor probes before planning; `set_plan` renders a graph and waits for your approval; a check question renders as a card and grades; locking a node lights the graph; and `/derive:review` on a later run offers the nodes that are due.
      2. Codex skills: in a Codex session with the Derive skills installed, run the `derive-learn` skill on any small topic and confirm the same sequence. This is the first run where the Codex skill has a real method body rather than tool descriptions alone — confirm the tutor teaches rather than only quizzing.
      3. Confirm nothing the learner sees has changed from before this phase: same cards, same graph, same phases, same wording of the refusal when you try to lock a node on a procedure question alone.

      Report anything that behaves differently from the pre-phase build, however small — and in particular anything that looks like an authentication failure in the companion page, since that is the one thing this gap closure changed underneath these two paths.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm test` reports `# fail 0` and at least 121 tests across at least 38 suites; the counts are written into the plan summary.
    - `pnpm --filter server exec node --import tsx --test test/wire-surface.test.ts test/mcp.test.ts` exits 0 with no line beginning `not ok`.
    - `git diff --quiet -- server/test/wire-surface.json` exits 0 — the tool wire surface is byte-identical across the whole gap closure.
    - `node scripts/check-method.mjs` exits 0 and prints `6 rendered copies match method/`.
    - `node scripts/doctor.mjs` exits 0 with no FAIL line and reports the install token at mode 0600.
    - `git diff --quiet -- web/` exits 0.
    - The plan summary records the server URL, the repository path and the doctor result, so the human run can start without setup.
    - The `<human-check>` above is recorded for end-of-phase harvest into the phase UAT, with its three steps intact.
  </acceptance_criteria>
  <done>The machine half of Success Criterion 1 is green after the authentication change, the machine is ready for the human run, and the three-step parity script is queued for the phase UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a reproduction command -> the learner's real data | a check run against `~/.derive` rather than a scratch directory would touch the learner's own lessons |
| a recorded result -> the re-verification pass | a row written down as passing that was never run is a false guarantee at the artifact level rather than the code level |
| the plugin and Codex header path -> the changed authentication boundary | plan 01-09 altered how credentials are accepted; the two terminal paths must still authenticate on the unchanged header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Repudiation | a spot-check recorded as passing without being run | high | mitigate | every row is recorded with the command and its observed output, and a row that cannot run on this machine is recorded with its reason rather than as a pass; the acceptance criteria name the exact commands |
| T-12-02 | Denial of service | the authentication change breaking the plugin or Codex header path | high | mitigate | `mcp.test.ts` drives the built stdio server through a whole lesson and `wire-surface.test.ts` pins all three surfaces; both are run and their counts recorded before the human run begins |
| T-12-03 | Tampering | a reproduction run against the learner's real `~/.derive` | medium | mitigate | every command in task 1 uses a scratch `DERIVE_DATA_DIR`; only the human run touches the real database, and it does so by using Derive normally |
| T-12-04 | Information disclosure | the install token pasted into a summary or a log while recording evidence | high | mitigate | the reproductions record status codes and match counts, never the token value; `registerSecret` already redacts it from errors and logs, and the human check compares the cookie to the token without writing either down |
| T-12-05 | Elevation of privilege | a second server left listening on `0.0.0.0` after the widened-bind reproduction | medium | mitigate | both spawned servers are killed as the task ends and the summary records that the port is free again; the wide server uses a random high port and a scratch data directory |
| T-12-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no dependency and installs nothing; it runs `pnpm build`, `pnpm test` and `curl` against what the previous plans produced. If execution finds it needs a package, stop and raise it rather than installing |
</threat_model>

<verification>
- Every ✗ FAIL row in `01-VERIFICATION.md`'s behavioural spot-check table re-runs as a pass, or is recorded as unrunnable with its reason.
- `pnpm typecheck`, `pnpm build`, `pnpm test`, `node scripts/check-method.mjs` and `node scripts/doctor.mjs` all green, with the test counts recorded.
- `git diff --quiet -- server/test/wire-surface.json` and `git diff --quiet -- web/` both exit 0 across the whole gap closure.
- No source file is modified by this plan: `git status --porcelain server/ scripts/ plugin/ method/` is empty after it.
- Both `<human-check>` blocks are recorded for end-of-phase harvest.

</verification>

<success_criteria>
- The authentication boundary, the clone guard and the deletion fix are demonstrated with the same instrument that found them broken (FOUND-06).
- FOUND-04's machine half is still green after the authentication change (wire surface unchanged, stdio MCP smoke passing).
- FOUND-04's human half — one real lesson through the plugin and one through the Codex skills — is queued as a `<human-check>`, sequenced after the Success Criterion 5 gaps closed, exactly as the verification requires.

</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-12-SUMMARY.md` when done
</output>
