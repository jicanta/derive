---
phase: 01-foundation
plan: 15
type: execute
wave: 3
depends_on: ["01-14"]
gap_closure: true
files_modified:
  - server/src/db.ts
  - server/test/tx.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, COST-01]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The ledger invariant db.ts:637-648 states — 'Every turn gets a usage row, whichever driver ran it' — survives a restart: a turn left running when the process died is closed with a usage row of nulls and cost_source 'unknown', not closed with no row at all."
    - "A turn that already reported usage is not given a second row by the boot sweep, because the close is idempotent on the turn id."
    - "tx.test.ts asserts the invariant with row counts rather than with an absence of throws: after a simulated restart sweep, the turn count and the usage count for the learner are equal."
    - "REQUIREMENTS.md's traceability rows for the seven Phase 1 requirement IDs state what the verification report's own Requirements Coverage table records, and the checkbox list at lines 14-19 matches those rows."
    - "The ROADMAP's Phase 1 cross-cutting constraint no longer asserts that web/ is untouched, because this gap run deliberately changes web/src/lib/api.ts and web/src/lib/useLesson.ts on the verification's own instruction."
    - "The two human-only items are carried forward as human verification with the exact script the verification report wrote, and neither is recorded as performed on any summary's word."
    - "Every advisory finding in 01-VERIFICATION.md is accounted for in writing: folded in and named, or left open and named with the reason."
  artifacts:
    - server/src/db.ts
    - server/test/tx.test.ts
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "a killed process -> closeOpenTurns('interrupted') -> closeUsage per swept turn -> the usage table -> listUsage, so turn counts and usage counts reconcile across every view"
    - "01-VERIFICATION.md Requirements Coverage table -> REQUIREMENTS.md traceability rows 143-148 and 167 -> the checkbox list at 14-19"
    - "01-VERIFICATION.md human_verification: -> this plan's human-check blocks -> the phase UAT harvest"
  prohibitions:
    - statement: "Never make a test or a comment pass by narrowing what it asserts or claims; correct the code, or correct the claim to what holds — this phase has already shipped green suites and in-repo sentences over guarantees the code did not deliver, and that is the failure being repaired."
      status: unverified
      verification: flagged
    - statement: "Never record a human-only check as performed on a summary's word; the plugin and Codex parity run and the real-browser smoke are done by a person or they stay open, and a status table that says otherwise is the same false guarantee in a different file."
      status: unverified
      verification: flagged
---

<objective>
**As the author** reading this project's own records, **I want to** find that every
status, constraint and comment says what actually holds, **so that** the next
phase starts from the truth rather than from a claim that has to be re-checked.

Two things are left after `01-13` and `01-14`.

The first is code: the boot sweep at `server/src/index.ts:69-71` closes every
turn a restart killed, but it does not give those turns a usage row — so every
restart breaks the invariant `server/src/db.ts:637-648` states in its own words,
"Every turn gets a usage row, whichever driver ran it." That invariant is what
lets Phase 4's usage page honestly say "six lessons on the Claude Code plugin,
tokens not reported" instead of quietly leaving them out. It was carried through
the first review as WR-02 and through the first verification unflagged, and it
is one line in the right place.

The second is the record. The verification report names REQUIREMENTS.md
accuracy as a deliverable: lines 143-148 and 167 all read `Gaps Found`, while
its own Requirements Coverage table records five of the seven as satisfied and
one as needing a human. The ROADMAP's Phase 1 cross-cutting constraint still
asserts that `git diff --quiet -- web/` exits 0, which `01-14` makes false on
the verification's own instruction. And the two human-only items — the Claude
Code plugin and Codex parity run, and the real-browser session smoke — must be
carried forward as human verification, never as tasks and never as done.

Purpose: COST-01's honest-ledger guarantee, and the accuracy of every status
this phase leaves behind.
Output: a ledger that reconciles across a restart, a traceability table that
matches the verification, a ROADMAP constraint that is true, and two human
checks queued with their scripts intact.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

Created by **this plan**: no new exported symbol. The change in
`server/src/db.ts` is one call added inside the existing `closeOpenTurns`, and
in `server/test/tx.test.ts` a case named for the restart invariant plus
`closeOpenTurns` added to the existing dynamic-import destructure. The changes
to `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are status and
constraint text, not new sections. No new file, no new dependency, no new env
var, no new migration.

## Edge coverage (spec-less probe) — flagged assumptions

The deterministic edge probe returned **17 applicable edges across the 7 phase
requirement IDs, all `unresolved`** (`resolved: 0`,
`byVerification: {explicit: 0, backstop: 0}`). An `unresolved` row is never
auto-resolved with a backstop; each is carried as an explicit flagged
assumption rather than dropped. FOUND-06's three rows are in `01-13-PLAN.md`
and FOUND-04's one row is in `01-14-PLAN.md`; the remaining **thirteen** are
recorded here. 3 + 1 + 13 = 17, the full probe set.

| Requirement | Category | Probe | Status | Assumption this run makes |
|---|---|---|---|---|
| FOUND-01 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | unresolved — flagged | Assumed: two registry entries whose names are equal cannot coexist, and the committed wire-surface fixture is the thing that would show it if they did. No source artifact states a collision rule; this run changes no registry entry and asserts the fixture is byte-identical. |
| FOUND-01 | empty | What is the result for empty, single-element, or null input? | unresolved — flagged | Assumed: a tool whose schema has no fields projects to an empty-object schema on all three surfaces rather than to an omitted schema. Not stated by any source artifact; unchanged and unexercised by this run. |
| FOUND-01 | encoding | Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form? | unresolved — flagged | Assumed: the wire-surface fixture compares tool descriptions as bytes, so a Unicode normalisation change in a description is a diff. Not stated by any source artifact; the `git diff --quiet` gate this run runs is a byte comparison, which is consistent with the assumption. |
| FOUND-01 | ordering | When elements compare equal, is output order specified and stable? | unresolved — flagged | Resolved in substance by an existing recorded decision rather than by the probe: STATE.md records that `tools/list` emits registry declaration order and that order is explicitly *not* the MCP contract. Carried as flagged because the probe could not see that decision, and because no case asserts stability under equal names. |
| FOUND-02 | empty | What is the result for empty, single-element, or null input? | unresolved — flagged | Assumed: an empty method section file is a render-time error rather than a silently empty region — the prior verification drove the removal of `method/50-checks.md` and got a named error, which is adjacent but not the same case. Not stated by any source artifact; unchanged by this run. |
| FOUND-02 | encoding | Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form? | unresolved — flagged | Assumed: `scripts/check-method.mjs` compares rendered copies byte for byte, so any encoding difference is drift. Not stated by any source artifact; this run runs that gate and asserts its "6 rendered copies match" line. |
| FOUND-03 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | unresolved — flagged | Assumed: two drivers registering the same lesson id collide rather than merge — `runTurn` throws `lesson is busy` when `active` already holds the lesson, which is the documented architectural constraint. Not restated as an acceptance criterion by any source artifact; unchanged by this run. |
| FOUND-03 | empty | What is the result for empty, single-element, or null input? | unresolved — flagged | Assumed: a driver that reports no events at all still produces a `turn_end` through the sink's idempotent guard. Not stated by any source artifact; the fake driver exercises the populated case only. |
| FOUND-03 | ordering | When elements compare equal, is output order specified and stable? | unresolved — flagged | Assumed: events with the same `seq` cannot occur, because `seq` is assigned by the append; ephemeral events use `seq: -1` and are not ordered against persisted ones. Not stated by any source artifact; unchanged by this run. |
| FOUND-05 | unclassified | unclassified — review manually | unresolved — flagged | Reviewed manually: the probe could not classify the migration requirement, so no edge predicate was derived from it. What this run does assert is the adjacent invariant it touches — that a restart leaves the `turns` and `usage` tables consistent — via the new `tx.test.ts` case. The migration runner itself is verified and untouched here. |
| COST-01 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | unresolved — flagged | Resolved in substance by this plan: two closes of the same turn id do not merge into two rows — `closeUsage` returns early when the turn already has usage — and the new case drives exactly that by sweeping a turn that already reported. Kept flagged because the probe produced no acceptance criterion of its own. |
| COST-01 | empty | What is the result for empty, single-element, or null input? | unresolved — flagged | Resolved in substance by this plan and by D-16: a turn with no reported counts gets a row of nulls with `cost_source` `'unknown'`, never an estimate and never an omission; the new case drives the swept-turn form of that. Kept flagged for the same reason. |
| COST-01 | ordering | When elements compare equal, is output order specified and stable? | unresolved — flagged | Assumed: usage rows are read back by `listUsage` in insertion order and nothing depends on a tie-break among rows sharing a timestamp. Not stated by any source artifact; this run's case counts rows rather than ordering them. |

<tasks>

<task type="tracer">
  <name>Task 1: a restart leaves the ledger reconciling, end to end</name>
  <reversibility rating="reversible">One call inside one function and one test case; reverting is deleting both. No schema change, no migration, and no row shape changes — a swept turn simply gains the row the invariant already promises it.</reversibility>
  <read_first>
    - `server/src/db.ts` lines 516-531: `openTurns()` and `closeOpenTurns(status)` in full, including its doc comment and the `withTx` wrapper it already runs inside
    - `server/src/db.ts` lines 600-650: `recordUsage`, `listUsage` and `closeUsage` in full, including the doc block at 636-648 that states the invariant in its own words and the early return that makes a second close a no-op
    - `server/src/index.ts` lines 63-72: the boot sweep's doc comment and the loop over `closeOpenTurns('interrupted')` — read it to confirm no change is needed there once the call moves into `closeOpenTurns`
    - `server/src/driver.ts` lines 105-120: the sink's `endTurn` path, which already calls `closeUsage(turnId)` on the normal end of a turn — the reason the added call has to be idempotent rather than unconditional
    - `server/test/tx.test.ts` lines 1-45: the suite doc comment ("an assertion that nothing threw would prove nothing"), the scratch-`DERIVE_DATA_DIR`-before-dynamic-import trick, and the destructured import list that `closeOpenTurns` must join
    - `server/test/tx.test.ts` lines 155-230: the `ledgerOf(learner.id)` helper and the cases that assert whole row-count objects with `assert.deepEqual`, which is the shape the new case must copy
    - `.planning/phases/01-foundation/01-VERIFICATION.md` `advisory:` finding 1 and its stated resolution ("moving the closeUsage call inside closeOpenTurns and asserting it in tx.test.ts")
    - `.planning/phases/01-foundation/01-CONTEXT.md` decisions D-15 and D-16, especially D-16's refusal to estimate tokens from transcript length
  </read_first>
  <files>server/src/db.ts, server/test/tx.test.ts</files>
  <action>
Give the turns a restart killed the usage row the ledger's own comment promises them.

In `server/src/db.ts`, inside `closeOpenTurns`, after the sweep statement runs and while still inside the existing `withTx` block, call `closeUsage` once for each swept row. Placing it here rather than at the caller is the point: `closeOpenTurns` is the one place a turn ends without a driver, so the invariant is enforced where it can be, not wherever a future caller remembers. `closeUsage` is already idempotent on the turn id — it returns early when the turn has usage — so a turn that reported before the process died keeps exactly the row it reported, and a turn that reported nothing gets one row of nulls with `cost_source` `'unknown'`. Per D-16 nothing is estimated and nothing is omitted.

Extend `closeOpenTurns`' existing doc comment with one sentence naming the reason — that a swept turn is still a turn, so it still owes the ledger a row — in the file's prose register, never a restatement of the call. Do not change `closeUsage`, `recordUsage`, the `turns` or `usage` schema, or the boot loop in `server/src/index.ts`, which needs no edit once the call lives here.

Then the case, in `server/test/tx.test.ts`, in the same commit. Add `closeOpenTurns` to the destructured dynamic import. Write a case that drives the real restart shape rather than a simulated one: create a learner and a lesson; start two turns; let one of them report usage through `recordUsage` and leave the other reporting nothing; call `closeOpenTurns('interrupted')`; then assert with `assert.deepEqual` on the whole `ledgerOf(learner.id)` object that the learner has two turns and two usage rows — so the count equality is the assertion, not the absence of a throw, exactly as the suite's doc comment demands. In the same case, assert that the turn which had already reported still has exactly one usage row after the sweep, so idempotence is driven rather than assumed, and assert that the row created for the silent turn carries `cost_source` `'unknown'` and null token counts rather than zeros.

Extend the suite doc comment with one clause naming what this case drives — that a sweep leaves the ledger reconciling — alongside the existing clause about row counts. Claim nothing the case does not drive.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" in the output</fails_when>
    <automated>pnpm --filter server exec node --import tsx --test test/tx.test.ts</automated>
    <fails_when>non-zero exit, or any line beginning "not ok" in the TAP output, or the summary line "# pass" reports 0</fails_when>
    <automated>pnpm --filter server exec node --import tsx --test test/usage.test.ts</automated>
    <fails_when>non-zero exit, or any line beginning "not ok" in the TAP output, or the summary line "# pass" reports 0</fails_when>
    <automated>pnpm test</automated>
    <fails_when>non-zero exit, or the node:test summary line "# fail" reports anything other than 0, or "# pass" reports fewer than the 144 tests this phase already had</fails_when>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter server exec node --import tsx --test test/tx.test.ts` exits 0 with no line beginning `not ok`, and its TAP output names the restart-sweep case.
    - The new case asserts a whole row-count object with `assert.deepEqual`, matching the existing `ledgerOf` cases — `grep -c 'ledgerOf' server/test/tx.test.ts` prints more occurrences than it did before this task.
    - Driven by hand on a scratch database, a turn swept by `closeOpenTurns('interrupted')` that never reported has exactly one usage row whose `cost_source` is `unknown` and whose token columns are null, and a turn that had reported keeps exactly the one row it reported.
    - `grep -c 'closeUsage' server/src/db.ts` prints at least `2` — the definition and the new call inside `closeOpenTurns`.
    - `pnpm test` reports `# fail 0` and `# pass` of at least 144, with the migration, driver and usage suites green.
  </acceptance_criteria>
  <done>A restart no longer breaks the invariant `server/src/db.ts` states: every turn the sweep closes carries a usage row, a turn that already reported keeps its own, nothing is estimated, and a case asserts the counts rather than the absence of an exception.</done>
</task>

<task type="auto">
  <name>Task 2: the record says what holds — requirement statuses, the ROADMAP constraint, and the two human checks</name>
  <reversibility rating="reversible">Status text and one constraint line in two planning documents; reverting is a git revert of one commit with no code effect.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 12-20: the Foundation checkbox list for FOUND-01..FOUND-06
    - `.planning/REQUIREMENTS.md` lines 139-170: the Traceability table header and the rows for FOUND-01..FOUND-06 (lines 143-148) and COST-01 (line 167), all of which currently read `Gaps Found`
    - `.planning/phases/01-foundation/01-VERIFICATION.md` § "Requirements Coverage" — the per-requirement table that records FOUND-01, FOUND-02, FOUND-03, FOUND-05 and COST-01 as ✓ SATISFIED, FOUND-04 as ⚠ PARTIAL / NEEDS HUMAN and FOUND-06 as ✗ BLOCKED — and the paragraph beginning "**REQUIREMENTS.md accuracy:**" immediately below it
    - `.planning/phases/01-foundation/01-VERIFICATION.md` § "Human Verification Required" items 1 and 2 in full, and the `human_verification:` block in the frontmatter, which carry the exact scripts to reproduce
    - `.planning/phases/01-foundation/01-VERIFICATION.md` `advisory:` — all six findings and their stated resolutions, which this task accounts for one by one
    - `.planning/ROADMAP.md` § "Phase 1: Foundation" — the **Plans** paragraph, the Gap-closure wave list, and the "**Cross-cutting constraints:**" bullet asserting `git diff --quiet -- web/` exits 0
    - `.planning/phases/01-foundation/01-13-PLAN.md` and `.planning/phases/01-foundation/01-14-PLAN.md` — for the plan titles and waves this task adds to the ROADMAP's gap-closure list
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Make the project's own records say what the verification found, and carry the two human-only items forward without marking either done.

In `.planning/REQUIREMENTS.md`, update the Traceability rows using the verification report's own Requirements Coverage table as the authority, not its prose summary — the prose names FOUND-01, FOUND-02 and FOUND-03 for promotion and omits FOUND-05 and COST-01, while its table records both of those as ✓ SATISFIED with evidence. Set FOUND-01, FOUND-02, FOUND-03, FOUND-05 and COST-01 to `Complete`; set FOUND-04 to `Needs Human`, because its machine half is proven and only the parity run is outstanding; and leave FOUND-06 at `Gaps Found`, because this gap run's work has not yet been verified and it is not this plan's place to pass its own phase. Note the prose/table discrepancy in the SUMMARY in one sentence, so the promotion of FOUND-05 and COST-01 is a stated reading of the evidence rather than a silent extension of the instruction.

Update the Foundation checkbox list at lines 14-19 so it matches those rows exactly: the five now-complete IDs are checked, FOUND-04 and FOUND-06 are not. Do not reword any requirement text.

In `.planning/ROADMAP.md`, § Phase 1: amend the "**Cross-cutting constraints:**" bullet. It currently asserts that `web/src/lib/useLesson.ts` is untouched and `git diff --quiet -- web/` exits 0. That was true of plans 01-09..01-12 and is deliberately false of `01-14`, which deletes the browser's dead token path on the verification's `missing:` item 6. Rewrite the bullet so it records what held and when: `web/` was untouched through the first gap wave, which is what let the credential change ship without a browser change; the second gap wave changes `web/src/lib/api.ts` and `web/src/lib/useLesson.ts` to delete a credential path the server no longer serves, and `noUnusedLocals` in `web/tsconfig.app.json` is the check that the deletion is complete. Leaving the old sentence standing would be the same defect this phase keeps repairing — a record asserting a property the tree does not have.

Still in the ROADMAP's Phase 1 section, confirm rather than re-add the gap-closure plan list: the planner already wrote the "Gap closure, second round" block with `01-13-PLAN.md` (gap wave 5), `01-14-PLAN.md` (gap wave 6) and `01-15-PLAN.md` (gap wave 7), and already updated the **Plans** paragraph and the Progress table to 12/15. If any of those three entries is missing, add it in the established format; otherwise leave them alone and only tick the checkboxes for the plans that have completed by the time this task runs. Edit only the Phase 1 section — use scoped replacements, never a whole-file write — and leave every other phase entry byte-identical.

Then the two human-only items. Neither becomes a task. Record both in the SUMMARY as outstanding human verification, carrying the verification report's own scripts and expectations verbatim rather than paraphrasing them, and noting for HC-2 that the verifier confirmed it unperformed by inspection — `~/.codex/skills` does not exist and `~/.claude/plugins/config.json` has no `derive` entry — so it is not to be recorded as done on any summary's word. Note for HC-2 that it should run after this gap run lands, because its first step opens the browser companion page whose credential path `01-14` changed.

Finally, account for every advisory finding in the SUMMARY, one line each, so none is silently dropped: findings 1 (boot sweep and the ledger), 3 (the redirect as a `Location`), 4 (`/api/health` under the shared guard), 2 (the Host check and a default port) and 5 (the `walk()` cycle guard) were folded into `01-13`, `01-14` and `01-15` task 1 and are named with the plan that closed them; finding 6 (`DELETE /api/learners/:id` leaving pending prompts, held cards and `recentCards` alive) is left open, because it is an in-memory lifecycle concern spanning `server/src/prompts.ts` and the `held` / `recentCards` maps rather than a front-door or ledger defect, the verifier rated it code-read-only with no reproduction, and folding it into a security gap-closure run would mix two unrelated changes in one diff. Record it as a carried concern for STATE.md rather than as closed.
  </action>
  <verify>
    <automated>grep -c 'Gaps Found' .planning/REQUIREMENTS.md</automated>
    <fails_when>the output is anything other than 1 — exactly one row, FOUND-06, may still carry that status; 7 means nothing was updated and 0 means FOUND-06 was promoted without verification</fails_when>
    <automated>grep -c 'Needs Human' .planning/REQUIREMENTS.md</automated>
    <fails_when>the output is anything other than 1 — FOUND-04 is the only row that may carry it, and 0 means the parity run was silently treated as done</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('.planning/ROADMAP.md','utf8');const n=(s.match(/^- \[[ x]\] \*\*Phase /gm)||[]).length;if(n!==7){console.error('roadmap phase list is '+n+', expected 7');process.exit(1)}console.log('roadmap phase list intact: '+n)"</automated>
    <fails_when>non-zero exit, or the output line reports a phase count other than 7 — which means a phase entry was dropped while editing the Phase 1 section</fails_when>
    <automated>git diff --stat -- .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
    <fails_when>the output is empty, which means neither document was edited at all, or it names a file outside those two</fails_when>
    <human-check>HC-2, the plugin and Codex parity run (FOUND-04, Success Criterion 1), carried forward verbatim from `01-VERIFICATION.md` § "Human Verification Required" item 1. (1) In a Claude Code session with the plugin installed from this repo, run `/derive:learn` on any small topic and confirm: the companion page opens; the tutor probes before planning; `set_plan` renders a graph and waits for approval; a check question renders as a card and grades; locking a node lights the graph; `/derive:review` on a later run offers the nodes that are due. (2) In a Codex session with the Derive skills installed, run `derive-learn` on any small topic and confirm the same sequence — this is the first run where the Codex skill has a real method body rather than tool descriptions alone, so confirm the tutor teaches rather than only quizzing. (3) Confirm nothing the learner sees changed: same cards, same graph, same phases, same wording of the refusal when locking a node on a procedure question alone. Expected: both lessons complete exactly as before the phase. Why human: needs a real model and a real login on two terminals, and the verifier confirmed it unperformed by inspection rather than on the summary's word — `~/.codex/skills` does not exist and `~/.claude/plugins/config.json` has no `derive` entry. Run it after this gap run lands, because step 1 opens the browser companion page whose credential path `01-14` changed.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FOUND-0[12356] | Phase 1' .planning/REQUIREMENTS.md` shows `Complete` for FOUND-01, FOUND-02, FOUND-03 and FOUND-05, and `grep -n 'COST-01 | Phase 1' .planning/REQUIREMENTS.md` shows `Complete`.
    - `grep -n 'FOUND-04 | Phase 1' .planning/REQUIREMENTS.md` shows `Needs Human`, and `grep -n 'FOUND-06 | Phase 1' .planning/REQUIREMENTS.md` still shows `Gaps Found`.
    - In `.planning/REQUIREMENTS.md` the Foundation checkbox list matches the table exactly: `sed -n '13,20p' .planning/REQUIREMENTS.md | grep -c '^- \[x\]'` prints `4` (FOUND-01, 02, 03, 05) and `sed -n '13,20p' .planning/REQUIREMENTS.md | grep -c '^- \[ \]'` prints `2` (FOUND-04, FOUND-06).
    - `grep -c 'git diff --quiet -- web/' .planning/ROADMAP.md` prints `0` — the constraint that this run makes false is gone, replaced by a sentence recording what held and when.
    - `grep -c '01-15-PLAN.md' .planning/ROADMAP.md` prints at least `1`, and `grep -c '01-13-PLAN.md' .planning/ROADMAP.md` and `grep -c '01-14-PLAN.md' .planning/ROADMAP.md` each print at least `1` — all three of this run's plans are listed with their waves.
    - The node check reports `roadmap phase list intact: 7`, so no phase entry was lost while editing the Phase 1 section.
    - The SUMMARY names HC-1 and HC-2 as outstanding with their scripts, and accounts for all six advisory findings by name — five folded in with the plan that closed each, one left open with its reason.
  </acceptance_criteria>
  <done>REQUIREMENTS.md states what the verification report's own coverage table found, the ROADMAP no longer asserts a constraint this run makes false and lists all three gap plans, and the two human-only checks are queued with their scripts intact and neither marked done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a killed process -> the usage ledger | a restart ends turns outside the driver, so the one place the invariant can be enforced is the sweep |
| the project's status records -> the phase that reads them next | a traceability row or a cross-cutting constraint that overstates is the same false-guarantee failure as a comment that overstates, in a file nobody re-checks |
| a human-only check -> a status table | recording an unperformed check as done is how an unverified path reaches production while looking verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Repudiation | the boot sweep closing turns without a usage row, so turn counts and usage counts disagree after every restart (`server/src/index.ts:69-71`, invariant at `server/src/db.ts:637-648`) | medium | mitigate | `closeUsage` is called for every swept row inside `closeOpenTurns`' existing transaction; `tx.test.ts` asserts the whole row-count object after a sweep, including the already-reported turn keeping exactly one row |
| T-15-02 | Tampering | a second usage row written for a turn that already reported, inflating the ledger | low | mitigate | `closeUsage` returns early when the turn has usage; the new case drives that path explicitly rather than assuming it |
| T-15-03 | Repudiation | a requirement status recorded as complete on prose rather than on evidence | medium | mitigate | the verification report's Requirements Coverage table is named as the authority, the prose/table discrepancy over FOUND-05 and COST-01 is recorded in the SUMMARY, and FOUND-06 is left at `Gaps Found` because this plan does not verify its own phase |
| T-15-04 | Repudiation | a human-only check silently promoted to done | high | mitigate | HC-1 and HC-2 are `<verify><human-check>` entries carrying the verifier's own scripts; the `Needs Human` grep gate fails if FOUND-04's row loses that status, and the standing prohibition forbids recording either on a summary's word |
| T-15-05 | Tampering | a whole-file rewrite of `.planning/ROADMAP.md` dropping phase entries outside the edit window | medium | mitigate | the edit is scoped to the Phase 1 section, and a node check asserts the roadmap still lists exactly seven phases |
| T-15-06 | Repudiation | an advisory finding disappearing between verification and the next phase | low | mitigate | all six are accounted for by name in the SUMMARY — five with the plan that closed them, one with the reason it stays open and a note to carry it into STATE.md |
| T-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | this plan adds no dependency; the only code change is one existing function call inside another existing function. If execution finds it needs a package, stop and raise it rather than installing |
</threat_model>

<verification>
- `pnpm typecheck` and `pnpm test` clean, with the full suite at 0 failures and no fewer passes than the 144 this phase already had.
- `pnpm --filter server exec node --import tsx --test test/tx.test.ts` green, including the restart-sweep case.
- `.planning/REQUIREMENTS.md` carries exactly one `Gaps Found` row (FOUND-06) and exactly one `Needs Human` row (FOUND-04), and the checkbox list matches.
- `.planning/ROADMAP.md` still lists seven phases, no longer asserts that `web/` is untouched, and lists `01-13`, `01-14` and `01-15` with their waves.
- HC-1 and HC-2 are recorded as outstanding with their scripts intact, and all six advisory findings are accounted for by name.

</verification>

<success_criteria>
- Every turn the boot sweep closes carries a usage row, so turn counts and usage counts reconcile across a restart (COST-01, and the invariant `server/src/db.ts` states).
- The traceability table and the checkbox list state what the verification report's own coverage table found, for all seven Phase 1 requirement IDs (FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06, COST-01).
- The ROADMAP's Phase 1 cross-cutting constraint is true of the tree this run leaves behind, and the gap-closure plan list is complete.
- Neither human-only check is recorded as performed, and both carry the verifier's own script (FOUND-04's human half).
- No advisory finding from `01-VERIFICATION.md` is dropped without a written reason.

</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-15-SUMMARY.md` when done
</output>