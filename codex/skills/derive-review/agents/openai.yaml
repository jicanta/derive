interface:
  display_name: "Derive: review"
  short_description: "Spaced-repetition review of the nodes that are due"
  default_prompt: "$derive-review"
dependencies:
  tools:
    - type: "mcp"
      value: "derive"
