{
  "name": "derive",
  "private": true,
  "version": "0.4.0",
  "description": "Learn anything from first principles. An AI tutor that builds your dependency graph of understanding, node by node.",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,magenta \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter web build && pnpm --filter server build",
    "start": "pnpm --filter server start",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter server test",
    "method": "node scripts/render-method.mjs",
    "method:check": "node scripts/check-method.mjs",
    "check": "node --env-file-if-exists=.env scripts/doctor.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "packageManager": "pnpm@12.5.1"
}
