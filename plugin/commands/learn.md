---
description: Learn a topic from first principles with Derive (probe -> plan -> teach, rendered live in the browser)
argument-hint: <topic you want to actually understand> [paths to course slides, PDFs, notes, a repo folder or a GitHub URL] [--terminal] [--learner <name>]
allowed-tools: mcp__plugin_derive_derive__start_lesson, mcp__plugin_derive_derive__attach_material, mcp__plugin_derive_derive__read_material, mcp__plugin_derive_derive__search_material, mcp__plugin_derive_derive__search_library, mcp__plugin_derive_derive__read_resource, mcp__plugin_derive_derive__suggest_resource, mcp__plugin_derive_derive__add_resource, mcp__plugin_derive_derive__quiz, mcp__plugin_derive_derive__ask, mcp__plugin_derive_derive__answer, mcp__plugin_derive_derive__answer_in, mcp__plugin_derive_derive__set_plan, mcp__plugin_derive_derive__node_status, mcp__plugin_derive_derive__set_phase, mcp__plugin_derive_derive__explain_back, mcp__plugin_derive_derive__remember, mcp__plugin_derive_derive__set_preferences, mcp__plugin_derive_derive__learner_profile, mcp__plugin_derive_derive__learners, mcp__plugin_derive_derive__end_lesson, WebSearch, WebFetch, Skill
---

Start a Derive lesson on: $ARGUMENTS

1. Load the `teach` skill from the derive plugin and follow it exactly. It is the only teaching method you use.
2. Call the derive `start_lesson` tool with the topic. If the argument names files, folders or URLs (course slides, a PDF, lecture notes: .pdf, .pptx, .docx, .md, .txt; a repository folder; a GitHub URL), pass them as `files`; the tool uploads or imports them and returns a brief of the material (its outline, or its full text when short) with instructions on using it. If the argument contains `--terminal` or the learner asks to answer here, pass `answer_in: "terminal"`; if it contains `--learner <name>`, pass `learner`. Strip those flags from the topic. `start_lesson` also opens the companion page in the browser and returns what is already known about this learner. Read both before probing: follow the learner's own preferences (language, style, pace, background, examples) in how you deliver, and if they tell you mid-lesson how they want to be taught, save it with `set_preferences`; build on locked nodes, re-derive shaky ones, watch for listed misconceptions, and when there is material, read the relevant pages or files with `read_material` before you plan so the plan covers what the course covers, in its notation. If the learner mentions files later, `attach_material` adds them mid-lesson. `start_lesson` also returns `warmup` when nodes from earlier lessons are due: do that first, exactly as it says (one fresh `quiz` per node with `purpose: "review"` and the given id, lock or mark shaky, no teaching), then the probe. It also returns `library` when the learner keeps one: the entries of their library relevant to the topic and the rules for using it (`search_library` before you plan, `read_resource` to borrow framing and examples, `suggest_resource` to point them at the right chapter or timestamp when a node locks, `add_resource` to save a source you found on the web, sparingly).
3. Run the lesson in the terminal: your prose here, every graded question through `quiz`, every open question through `ask`, the plan through `set_plan`, node states through `node_status`. Where the learner answers depends on `answer_in`, which `start_lesson` returns:
   - **browser**: each of those tools blocks until the learner answers in the browser and returns their answer.
   - **terminal**: each of those tools returns the card as text at once. Show it verbatim as your message, then end your turn. The learner's next message is their reply: pass it, verbatim, to `answer` before anything else; it returns the graded result. Never answer for them, never continue past an open card, never reveal an explanation before `answer` has returned. If they answered in the browser meanwhile, `answer` returns that.
4. This is a lesson, not a coding task, so your usual brevity does not apply. For a derived node, first ask for their attempt (`quiz` with `purpose: "pretest"`; a miss there is expected and is not held against them), then write the actual teaching: motivate the node, establish it from the nodes below it, make the dependency explicit. Several short paragraphs with math, a table or a diagram where they earn their place. Then the check (`purpose: "check"`, a different question). A bare sequence of quizzes with one-line remarks between them is a failed lesson. Only skip the teaching for a node the probe already showed the learner holds: say so in one sentence and pass `already_held: true` to `quiz`. Otherwise the check is refused until the teaching has been written. Every quiz result says how sure the learner was and what to do next; do that (a hint before a re-derivation, a teach-back after an unsure pass, a named correction after a confident miss). Never end your turn in the teach phase without a card pending: write the teaching and call `quiz` in the same turn.
5. Pass `tests` on every teach-phase quiz (`intuition`, `procedure` or `transfer`). A derived node does not lock on procedure alone: `node_status(id, "locked")` is refused until an intuition or transfer question on it has passed, so ask one (why it must be so, what breaks if a premise changes, the right picture, a problem of a kind not shown) before locking. Most checks should be intuition questions; the learner's stated weakness is memorizing steps without understanding.
6. When the goal node is locked, `node_status` returns the cumulative quiz: one fresh question per node, `purpose: "cumulative"`, mixed order, at least half transfer. Run it in full; a miss marks the node shaky and the result says how to remediate. Only when every node has held, write the closing (the compressed version of the whole graph), store 1 to 3 durable notes with `remember`, then call `end_lesson`.

If `start_lesson` fails because the server is not running, tell the learner to run `pnpm start` (or `pnpm dev`) in the derive repo and stop.
