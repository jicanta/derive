{
  "agent": [
    {
      "name": "quiz",
      "description": "Ask the learner ONE graded multiple-choice question with a known correct answer. The app renders the options, the learner picks and says whether they are sure, the app grades it and reveals your explanation. Returns what they picked, whether it was correct, how sure they were, and what to do next. Set `purpose`: \"pretest\" for the attempt you ask for BEFORE teaching a derived node (a miss is expected and is not recorded against them), \"check\" for the question that locks a node, \"cumulative\" for the end-of-lesson quiz; the probe phase and reviews of copied nodes are recognised on their own. Set `tests` to what the question tests (intuition, procedure, transfer): a derived node locks only after a correct intuition or transfer question. Blocks until the learner answers.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "The question, markdown with $LaTeX$ allowed. Do not restate it in prose."
          },
          "options": {
            "minItems": 2,
            "maxItems": 3,
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "2 or 3 bare claims, no justification. The app adds \"I don't know\" itself."
          },
          "correct": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 0,
              "maximum": 9007199254740991
            },
            "description": "0-based indices of the correct option(s). Usually exactly one."
          },
          "explanation": {
            "type": "string",
            "description": "Why the correct answer is correct, and what each distractor gets wrong. Shown only after answering."
          },
          "node_id": {
            "description": "The plan node this question checks. Always pass it in the teach phase.",
            "type": "string"
          },
          "purpose": {
            "description": "\"pretest\": the attempt before teaching a node (not recorded against the learner, never locks). \"check\": the question that locks a node. \"cumulative\": the end-of-lesson quiz over every node, after the goal locks. \"review\": a node from an earlier lesson (the warm-up, or a review session). Default: \"review\" on a copied node, \"probe\" in the probe phase, else \"check\".",
            "type": "string",
            "enum": [
              "probe",
              "pretest",
              "check",
              "cumulative",
              "review"
            ]
          },
          "tests": {
            "description": "What the question tests. \"intuition\": why the claim must be so, what breaks if a premise changes, which picture or geometric reading is right, an estimate before any computation. \"procedure\": carry out the steps. \"transfer\": a problem of a kind this lesson has not shown. A derived node locks only after a correct intuition or transfer question; always set this in the teach phase.",
            "type": "string",
            "enum": [
              "intuition",
              "procedure",
              "transfer"
            ]
          }
        },
        "required": [
          "question",
          "options",
          "correct",
          "explanation"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "ask",
      "description": "Ask the learner a question with no right answer (goal, preference, energy, what next). Optionally offer choices; the learner can always type a free answer. Blocks until they answer.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string"
          },
          "options": {
            "description": "Optional suggested answers.",
            "maxItems": 4,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "question"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_plan",
      "description": "Submit the lesson plan as a dependency DAG: unconditional truths at the roots (kind \"truth\"), derived steps (kind \"derived\"), exactly one \"goal\" sink. The app draws it and asks the learner to approve. Blocks until they approve or request changes; if they request changes, revise and call again.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "goal": {
            "type": "string",
            "description": "The learning goal in one sentence, as agreed with the learner."
          },
          "nodes": {
            "minItems": 3,
            "maxItems": 12,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "description": "Short stable id, e.g. \"packets\"."
                },
                "label": {
                  "type": "string",
                  "description": "The claim in plain words a learner reads at a glance, 3 to 7 words, e.g. \"A line can output any real number\". No formulas, symbols, abbreviations or private shorthand: this is what the graph shows."
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "truth",
                    "derived",
                    "goal"
                  ]
                },
                "summary": {
                  "type": "string",
                  "description": "One full sentence stating the claim this node stands for. Shown to the learner next to the label; write it for them."
                },
                "depends_on": {
                  "description": "Ids of the nodes this one is derived from. Empty for roots.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "id",
                "label",
                "kind",
                "summary"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "goal",
          "nodes"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "node_status",
      "description": "Update the state of a plan node: \"teaching\" when you start it, \"locked\" when a confident check confirmed it (the node is then scheduled for review by how well the check went; the reply says in how many days, and which nodes below it earned implicit review credit), \"shaky\" when it did not land after two checks (the reply names the nodes it rests on and what the checks on each showed, for targeted remediation). Locking a derived node is refused until a correct intuition or transfer question on it exists; locking the goal returns the instructions for the cumulative quiz.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "teaching",
              "locked",
              "shaky"
            ]
          }
        },
        "required": [
          "id",
          "status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_phase",
      "description": "Announce which phase of the lesson you are in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "phase": {
            "type": "string",
            "enum": [
              "probe",
              "plan",
              "teach"
            ]
          }
        },
        "required": [
          "phase"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "explain_back",
      "description": "Teach-back check: ask the learner to explain a node in their own words (2 to 5 sentences), or to say WHY a claim must be true. Write the rubric first: the 2 or 3 things a correct explanation must contain. Returns their explanation for you to grade. Use it at least once per lesson on the most important derived node, and whenever a pass was unsure. Blocks until they write.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "What to explain, e.g. \"Explain in your own words why the step size has to be below 2/L.\""
          },
          "rubric": {
            "type": "string",
            "description": "The 2 or 3 things a correct explanation must contain. Not shown to the learner."
          },
          "node_id": {
            "type": "string"
          }
        },
        "required": [
          "prompt",
          "rubric"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "remember",
      "description": "Store one durable fact about this learner for future lessons: a strength, a gap, a preference (Socratic vs narrated), a background detail. One sentence. Use sparingly: 1 to 3 per lesson.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "fact": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "learner",
              "preference",
              "strength",
              "gap"
            ]
          }
        },
        "required": [
          "fact"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_preferences",
      "description": "Update how this learner wants to be taught, for this and every future lesson: the language to write in, how Socratic (style), how long each step runs (pace), their background, how they learn in their words, and where to take examples from. Call it when the learner TELLS you how they want to be taught (\"en español por favor\", \"just explain it, stop quizzing me through every step\", \"I'm a musician, use music\"), passing only the fields they touched, in their words. Do not infer it from a single reaction; that is what `remember` is for. An empty string clears a field.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "language": {
            "description": "The language to teach in, e.g. \"Spanish\". Empty string: the language the learner writes in.",
            "type": "string"
          },
          "style": {
            "type": "string",
            "enum": [
              "adaptive",
              "socratic",
              "narrated"
            ]
          },
          "pace": {
            "type": "string",
            "enum": [
              "brisk",
              "standard",
              "thorough"
            ]
          },
          "background": {
            "description": "Who they are and what they already know, in their words.",
            "type": "string"
          },
          "how": {
            "description": "What works for them and what does not, in their words.",
            "type": "string"
          },
          "examples": {
            "description": "Domains to draw examples and analogies from.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "read_material",
      "description": "Read a range of the course material the learner attached (pages of a PDF, slides of a deck, parts of a document, files of a repository). Returns the text with a marker before each page, slide or file. For a repository pass `path` to read one file. Read the relevant range before planning and before teaching a node that maps to it. About ten pages per call. Only useful when the lesson has material (listed in your instructions, or announced in a tool result).",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "description": "Which material, by name (or part of it). Optional when only one is attached.",
            "type": "string"
          },
          "path": {
            "description": "Repository material only: the file to read, by path (exact, or a suffix such as \"src/db.ts\").",
            "type": "string"
          },
          "from": {
            "description": "First page, slide or file, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last page, slide or file, inclusive. Default: from + 9 (or the one file, with path).",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_material",
      "description": "Find where something is covered in the attached course material. Returns the best-matching pages, slides or files with a snippet each. Use it to locate a definition, an example, a formula or a function before you read the range around it. Only useful when the lesson has material.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the term, symbol or example you are looking for."
          },
          "name": {
            "description": "Restrict to one file. Default: all attached material.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "search_library",
      "description": "Search the learner's library: the articles, videos, books, papers, courses and notes they keep across lessons (listed in your instructions when there are any). Matches titles, tags, notes and the fetched text. Returns entries with a snippet and the part it was found in. Search it before you plan, and when the learner asks for something to read or watch.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the topic, a term, an author. Empty lists the shelf."
          },
          "kind": {
            "description": "Restrict to one kind.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "tag": {
            "description": "Restrict to one tag.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "read_resource",
      "description": "Read a range of parts of one library entry (an article's body, a paper's PDF, a video's description). About ten parts per call, with a marker before each. Pass the entry's id (from the catalog or a search hit) or its title. An entry with nothing fetched returns its URL and note; fetch the URL yourself then.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "from": {
            "description": "First part, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last part, inclusive. Default from + 9.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "suggest_resource",
      "description": "Point the learner at one entry of their library, as a card in the lesson: which entry, why it is worth their time now, and where to look (a chapter, a section, a timestamp). Use it when a node locks and the entry deepens it, when the learner wants more, or when a source explains a step better than chat can. One at a time, only when it earns its place. Only entries in the library or ones you just saved with add_resource.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "why": {
            "type": "string",
            "description": "One or two sentences, to the learner: what this gives them that the lesson did not."
          },
          "where": {
            "description": "Where to look: \"chapter 3\", \"from 12:40\", \"the section on invariants\".",
            "type": "string"
          },
          "node_id": {
            "description": "The plan node it deepens, if any.",
            "type": "string"
          }
        },
        "required": [
          "why"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "add_resource",
      "description": "Save a source to the learner's library for later: a URL you found with a web search or read (the page is fetched and its text kept), with a one-sentence note on why and a few tags. Sparingly: one or two per lesson, and only sources you actually read. A URL already on the shelf is not duplicated; your note and tags are merged in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The page, video, paper or book to save."
          },
          "title": {
            "description": "Override the fetched title.",
            "type": "string"
          },
          "kind": {
            "description": "Guessed from the URL when omitted.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "author": {
            "type": "string"
          },
          "note": {
            "type": "string",
            "description": "One sentence, to the learner: why this is worth keeping."
          },
          "tags": {
            "description": "A few lowercase tags, e.g. [\"calculus\", \"visual\"].",
            "maxItems": 8,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "url",
          "note"
        ],
        "additionalProperties": false
      }
    }
  ],
  "mcp": [
    {
      "name": "quiz",
      "description": "Ask the learner ONE graded multiple-choice question with a known correct answer. The app renders the options, the learner picks and says whether they are sure, the app grades it and reveals your explanation. Returns what they picked, whether it was correct, how sure they were, and what to do next. Set `purpose`: \"pretest\" for the attempt you ask for BEFORE teaching a derived node (a miss is expected and is not recorded against them), \"check\" for the question that locks a node, \"cumulative\" for the end-of-lesson quiz; the probe phase and reviews of copied nodes are recognised on their own. Set `tests` to what the question tests (intuition, procedure, transfer): a derived node locks only after a correct intuition or transfer question. Blocks until the learner answers.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "The question, markdown with $LaTeX$ allowed. Do not restate it in prose."
          },
          "options": {
            "minItems": 2,
            "maxItems": 3,
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "2 or 3 bare claims, no justification. The app adds \"I don't know\" itself."
          },
          "correct": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 0,
              "maximum": 9007199254740991
            },
            "description": "0-based indices of the correct option(s). Usually exactly one."
          },
          "explanation": {
            "type": "string",
            "description": "Why the correct answer is correct, and what each distractor gets wrong. Shown only after answering."
          },
          "node_id": {
            "description": "The plan node this question checks. Always pass it in the teach phase.",
            "type": "string"
          },
          "purpose": {
            "description": "\"pretest\": the attempt before teaching a node (not recorded against the learner, never locks). \"check\": the question that locks a node. \"cumulative\": the end-of-lesson quiz over every node, after the goal locks. \"review\": a node from an earlier lesson (the warm-up, or a review session). Default: \"review\" on a copied node, \"probe\" in the probe phase, else \"check\".",
            "type": "string",
            "enum": [
              "probe",
              "pretest",
              "check",
              "cumulative",
              "review"
            ]
          },
          "tests": {
            "description": "What the question tests. \"intuition\": why the claim must be so, what breaks if a premise changes, which picture or geometric reading is right, an estimate before any computation. \"procedure\": carry out the steps. \"transfer\": a problem of a kind this lesson has not shown. A derived node locks only after a correct intuition or transfer question; always set this in the teach phase.",
            "type": "string",
            "enum": [
              "intuition",
              "procedure",
              "transfer"
            ]
          }
        },
        "required": [
          "question",
          "options",
          "correct",
          "explanation"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "ask",
      "description": "Ask the learner a question with no right answer (goal, preference, energy, what next). Optionally offer choices; the learner can always type a free answer. Blocks until they answer.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string"
          },
          "options": {
            "description": "Optional suggested answers.",
            "maxItems": 4,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "question"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_plan",
      "description": "Submit the lesson plan as a dependency DAG: unconditional truths at the roots (kind \"truth\"), derived steps (kind \"derived\"), exactly one \"goal\" sink. The app draws it and asks the learner to approve. Blocks until they approve or request changes; if they request changes, revise and call again.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "goal": {
            "type": "string",
            "description": "The learning goal in one sentence, as agreed with the learner."
          },
          "nodes": {
            "minItems": 3,
            "maxItems": 12,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "description": "Short stable id, e.g. \"packets\"."
                },
                "label": {
                  "type": "string",
                  "description": "The claim in plain words a learner reads at a glance, 3 to 7 words, e.g. \"A line can output any real number\". No formulas, symbols, abbreviations or private shorthand: this is what the graph shows."
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "truth",
                    "derived",
                    "goal"
                  ]
                },
                "summary": {
                  "type": "string",
                  "description": "One full sentence stating the claim this node stands for. Shown to the learner next to the label; write it for them."
                },
                "depends_on": {
                  "description": "Ids of the nodes this one is derived from. Empty for roots.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "id",
                "label",
                "kind",
                "summary"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "goal",
          "nodes"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "node_status",
      "description": "Update the state of a plan node: \"teaching\" when you start it, \"locked\" when a confident check confirmed it (the node is then scheduled for review by how well the check went; the reply says in how many days, and which nodes below it earned implicit review credit), \"shaky\" when it did not land after two checks (the reply names the nodes it rests on and what the checks on each showed, for targeted remediation). Locking a derived node is refused until a correct intuition or transfer question on it exists; locking the goal returns the instructions for the cumulative quiz.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "teaching",
              "locked",
              "shaky"
            ]
          }
        },
        "required": [
          "id",
          "status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_phase",
      "description": "Announce which phase of the lesson you are in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "phase": {
            "type": "string",
            "enum": [
              "probe",
              "plan",
              "teach"
            ]
          }
        },
        "required": [
          "phase"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "explain_back",
      "description": "Teach-back check: ask the learner to explain a node in their own words (2 to 5 sentences), or to say WHY a claim must be true. Write the rubric first: the 2 or 3 things a correct explanation must contain. Returns their explanation for you to grade. Use it at least once per lesson on the most important derived node, and whenever a pass was unsure. Blocks until they write.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "What to explain, e.g. \"Explain in your own words why the step size has to be below 2/L.\""
          },
          "rubric": {
            "type": "string",
            "description": "The 2 or 3 things a correct explanation must contain. Not shown to the learner."
          },
          "node_id": {
            "type": "string"
          }
        },
        "required": [
          "prompt",
          "rubric"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "remember",
      "description": "Store one durable fact about this learner for future lessons: a strength, a gap, a preference (Socratic vs narrated), a background detail. One sentence. Use sparingly: 1 to 3 per lesson.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "fact": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "learner",
              "preference",
              "strength",
              "gap"
            ]
          }
        },
        "required": [
          "fact"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_preferences",
      "description": "Update how this learner wants to be taught, for this and every future lesson: the language to write in, how Socratic (style), how long each step runs (pace), their background, how they learn in their words, and where to take examples from. Call it when the learner TELLS you how they want to be taught (\"en español por favor\", \"just explain it, stop quizzing me through every step\", \"I'm a musician, use music\"), passing only the fields they touched, in their words. Do not infer it from a single reaction; that is what `remember` is for. An empty string clears a field.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "language": {
            "description": "The language to teach in, e.g. \"Spanish\". Empty string: the language the learner writes in.",
            "type": "string"
          },
          "style": {
            "type": "string",
            "enum": [
              "adaptive",
              "socratic",
              "narrated"
            ]
          },
          "pace": {
            "type": "string",
            "enum": [
              "brisk",
              "standard",
              "thorough"
            ]
          },
          "background": {
            "description": "Who they are and what they already know, in their words.",
            "type": "string"
          },
          "how": {
            "description": "What works for them and what does not, in their words.",
            "type": "string"
          },
          "examples": {
            "description": "Domains to draw examples and analogies from.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "read_material",
      "description": "Read a range of the course material the learner attached (pages of a PDF, slides of a deck, parts of a document, files of a repository). Returns the text with a marker before each page, slide or file. For a repository pass `path` to read one file. Read the relevant range before planning and before teaching a node that maps to it. About ten pages per call. Only useful when the lesson has material (listed in your instructions, or announced in a tool result).",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "description": "Which material, by name (or part of it). Optional when only one is attached.",
            "type": "string"
          },
          "path": {
            "description": "Repository material only: the file to read, by path (exact, or a suffix such as \"src/db.ts\").",
            "type": "string"
          },
          "from": {
            "description": "First page, slide or file, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last page, slide or file, inclusive. Default: from + 9 (or the one file, with path).",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_material",
      "description": "Find where something is covered in the attached course material. Returns the best-matching pages, slides or files with a snippet each. Use it to locate a definition, an example, a formula or a function before you read the range around it. Only useful when the lesson has material.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the term, symbol or example you are looking for."
          },
          "name": {
            "description": "Restrict to one file. Default: all attached material.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "search_library",
      "description": "Search the learner's library: the articles, videos, books, papers, courses and notes they keep across lessons (listed in your instructions when there are any). Matches titles, tags, notes and the fetched text. Returns entries with a snippet and the part it was found in. Search it before you plan, and when the learner asks for something to read or watch.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the topic, a term, an author. Empty lists the shelf."
          },
          "kind": {
            "description": "Restrict to one kind.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "tag": {
            "description": "Restrict to one tag.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "read_resource",
      "description": "Read a range of parts of one library entry (an article's body, a paper's PDF, a video's description). About ten parts per call, with a marker before each. Pass the entry's id (from the catalog or a search hit) or its title. An entry with nothing fetched returns its URL and note; fetch the URL yourself then.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "from": {
            "description": "First part, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last part, inclusive. Default from + 9.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "suggest_resource",
      "description": "Point the learner at one entry of their library, as a card in the lesson: which entry, why it is worth their time now, and where to look (a chapter, a section, a timestamp). Use it when a node locks and the entry deepens it, when the learner wants more, or when a source explains a step better than chat can. One at a time, only when it earns its place. Only entries in the library or ones you just saved with add_resource.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "why": {
            "type": "string",
            "description": "One or two sentences, to the learner: what this gives them that the lesson did not."
          },
          "where": {
            "description": "Where to look: \"chapter 3\", \"from 12:40\", \"the section on invariants\".",
            "type": "string"
          },
          "node_id": {
            "description": "The plan node it deepens, if any.",
            "type": "string"
          }
        },
        "required": [
          "why"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "add_resource",
      "description": "Save a source to the learner's library for later: a URL you found with a web search or read (the page is fetched and its text kept), with a one-sentence note on why and a few tags. Sparingly: one or two per lesson, and only sources you actually read. A URL already on the shelf is not duplicated; your note and tags are merged in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The page, video, paper or book to save."
          },
          "title": {
            "description": "Override the fetched title.",
            "type": "string"
          },
          "kind": {
            "description": "Guessed from the URL when omitted.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "author": {
            "type": "string"
          },
          "note": {
            "type": "string",
            "description": "One sentence, to the learner: why this is worth keeping."
          },
          "tags": {
            "description": "A few lowercase tags, e.g. [\"calculus\", \"visual\"].",
            "maxItems": 8,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "url",
          "note"
        ],
        "additionalProperties": false
      }
    }
  ],
  "http": [
    {
      "name": "quiz",
      "description": "Ask the learner ONE graded multiple-choice question with a known correct answer. The app renders the options, the learner picks and says whether they are sure, the app grades it and reveals your explanation. Returns what they picked, whether it was correct, how sure they were, and what to do next. Set `purpose`: \"pretest\" for the attempt you ask for BEFORE teaching a derived node (a miss is expected and is not recorded against them), \"check\" for the question that locks a node, \"cumulative\" for the end-of-lesson quiz; the probe phase and reviews of copied nodes are recognised on their own. Set `tests` to what the question tests (intuition, procedure, transfer): a derived node locks only after a correct intuition or transfer question. Blocks until the learner answers.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "The question, markdown with $LaTeX$ allowed. Do not restate it in prose."
          },
          "options": {
            "minItems": 2,
            "maxItems": 3,
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "2 or 3 bare claims, no justification. The app adds \"I don't know\" itself."
          },
          "correct": {
            "minItems": 1,
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 0,
              "maximum": 9007199254740991
            },
            "description": "0-based indices of the correct option(s). Usually exactly one."
          },
          "explanation": {
            "type": "string",
            "description": "Why the correct answer is correct, and what each distractor gets wrong. Shown only after answering."
          },
          "node_id": {
            "description": "The plan node this question checks. Always pass it in the teach phase.",
            "type": "string"
          },
          "purpose": {
            "description": "\"pretest\": the attempt before teaching a node (not recorded against the learner, never locks). \"check\": the question that locks a node. \"cumulative\": the end-of-lesson quiz over every node, after the goal locks. \"review\": a node from an earlier lesson (the warm-up, or a review session). Default: \"review\" on a copied node, \"probe\" in the probe phase, else \"check\".",
            "type": "string",
            "enum": [
              "probe",
              "pretest",
              "check",
              "cumulative",
              "review"
            ]
          },
          "tests": {
            "description": "What the question tests. \"intuition\": why the claim must be so, what breaks if a premise changes, which picture or geometric reading is right, an estimate before any computation. \"procedure\": carry out the steps. \"transfer\": a problem of a kind this lesson has not shown. A derived node locks only after a correct intuition or transfer question; always set this in the teach phase.",
            "type": "string",
            "enum": [
              "intuition",
              "procedure",
              "transfer"
            ]
          }
        },
        "required": [
          "question",
          "options",
          "correct",
          "explanation"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "ask",
      "description": "Ask the learner a question with no right answer (goal, preference, energy, what next). Optionally offer choices; the learner can always type a free answer. Blocks until they answer.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "question": {
            "type": "string"
          },
          "options": {
            "description": "Optional suggested answers.",
            "maxItems": 4,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "question"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_plan",
      "description": "Submit the lesson plan as a dependency DAG: unconditional truths at the roots (kind \"truth\"), derived steps (kind \"derived\"), exactly one \"goal\" sink. The app draws it and asks the learner to approve. Blocks until they approve or request changes; if they request changes, revise and call again.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "goal": {
            "type": "string",
            "description": "The learning goal in one sentence, as agreed with the learner."
          },
          "nodes": {
            "minItems": 3,
            "maxItems": 12,
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "string",
                  "description": "Short stable id, e.g. \"packets\"."
                },
                "label": {
                  "type": "string",
                  "description": "The claim in plain words a learner reads at a glance, 3 to 7 words, e.g. \"A line can output any real number\". No formulas, symbols, abbreviations or private shorthand: this is what the graph shows."
                },
                "kind": {
                  "type": "string",
                  "enum": [
                    "truth",
                    "derived",
                    "goal"
                  ]
                },
                "summary": {
                  "type": "string",
                  "description": "One full sentence stating the claim this node stands for. Shown to the learner next to the label; write it for them."
                },
                "depends_on": {
                  "description": "Ids of the nodes this one is derived from. Empty for roots.",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              },
              "required": [
                "id",
                "label",
                "kind",
                "summary"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "goal",
          "nodes"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "node_status",
      "description": "Update the state of a plan node: \"teaching\" when you start it, \"locked\" when a confident check confirmed it (the node is then scheduled for review by how well the check went; the reply says in how many days, and which nodes below it earned implicit review credit), \"shaky\" when it did not land after two checks (the reply names the nodes it rests on and what the checks on each showed, for targeted remediation). Locking a derived node is refused until a correct intuition or transfer question on it exists; locking the goal returns the instructions for the cumulative quiz.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "status": {
            "type": "string",
            "enum": [
              "teaching",
              "locked",
              "shaky"
            ]
          }
        },
        "required": [
          "id",
          "status"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_phase",
      "description": "Announce which phase of the lesson you are in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "phase": {
            "type": "string",
            "enum": [
              "probe",
              "plan",
              "teach"
            ]
          }
        },
        "required": [
          "phase"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "explain_back",
      "description": "Teach-back check: ask the learner to explain a node in their own words (2 to 5 sentences), or to say WHY a claim must be true. Write the rubric first: the 2 or 3 things a correct explanation must contain. Returns their explanation for you to grade. Use it at least once per lesson on the most important derived node, and whenever a pass was unsure. Blocks until they write.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "What to explain, e.g. \"Explain in your own words why the step size has to be below 2/L.\""
          },
          "rubric": {
            "type": "string",
            "description": "The 2 or 3 things a correct explanation must contain. Not shown to the learner."
          },
          "node_id": {
            "type": "string"
          }
        },
        "required": [
          "prompt",
          "rubric"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "remember",
      "description": "Store one durable fact about this learner for future lessons: a strength, a gap, a preference (Socratic vs narrated), a background detail. One sentence. Use sparingly: 1 to 3 per lesson.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "fact": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "learner",
              "preference",
              "strength",
              "gap"
            ]
          }
        },
        "required": [
          "fact"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "set_preferences",
      "description": "Update how this learner wants to be taught, for this and every future lesson: the language to write in, how Socratic (style), how long each step runs (pace), their background, how they learn in their words, and where to take examples from. Call it when the learner TELLS you how they want to be taught (\"en español por favor\", \"just explain it, stop quizzing me through every step\", \"I'm a musician, use music\"), passing only the fields they touched, in their words. Do not infer it from a single reaction; that is what `remember` is for. An empty string clears a field.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "language": {
            "description": "The language to teach in, e.g. \"Spanish\". Empty string: the language the learner writes in.",
            "type": "string"
          },
          "style": {
            "type": "string",
            "enum": [
              "adaptive",
              "socratic",
              "narrated"
            ]
          },
          "pace": {
            "type": "string",
            "enum": [
              "brisk",
              "standard",
              "thorough"
            ]
          },
          "background": {
            "description": "Who they are and what they already know, in their words.",
            "type": "string"
          },
          "how": {
            "description": "What works for them and what does not, in their words.",
            "type": "string"
          },
          "examples": {
            "description": "Domains to draw examples and analogies from.",
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "read_material",
      "description": "Read a range of the course material the learner attached (pages of a PDF, slides of a deck, parts of a document, files of a repository). Returns the text with a marker before each page, slide or file. For a repository pass `path` to read one file. Read the relevant range before planning and before teaching a node that maps to it. About ten pages per call. Only useful when the lesson has material (listed in your instructions, or announced in a tool result).",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "name": {
            "description": "Which material, by name (or part of it). Optional when only one is attached.",
            "type": "string"
          },
          "path": {
            "description": "Repository material only: the file to read, by path (exact, or a suffix such as \"src/db.ts\").",
            "type": "string"
          },
          "from": {
            "description": "First page, slide or file, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last page, slide or file, inclusive. Default: from + 9 (or the one file, with path).",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_material",
      "description": "Find where something is covered in the attached course material. Returns the best-matching pages, slides or files with a snippet each. Use it to locate a definition, an example, a formula or a function before you read the range around it. Only useful when the lesson has material.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the term, symbol or example you are looking for."
          },
          "name": {
            "description": "Restrict to one file. Default: all attached material.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "search_library",
      "description": "Search the learner's library: the articles, videos, books, papers, courses and notes they keep across lessons (listed in your instructions when there are any). Matches titles, tags, notes and the fetched text. Returns entries with a snippet and the part it was found in. Search it before you plan, and when the learner asks for something to read or watch.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "A few words: the topic, a term, an author. Empty lists the shelf."
          },
          "kind": {
            "description": "Restrict to one kind.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "tag": {
            "description": "Restrict to one tag.",
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20
          }
        },
        "required": [
          "query"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "read_resource",
      "description": "Read a range of parts of one library entry (an article's body, a paper's PDF, a video's description). About ten parts per call, with a marker before each. Pass the entry's id (from the catalog or a search hit) or its title. An entry with nothing fetched returns its URL and note; fetch the URL yourself then.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "from": {
            "description": "First part, 1-based. Default 1.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          },
          "to": {
            "description": "Last part, inclusive. Default from + 9.",
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "suggest_resource",
      "description": "Point the learner at one entry of their library, as a card in the lesson: which entry, why it is worth their time now, and where to look (a chapter, a section, a timestamp). Use it when a node locks and the entry deepens it, when the learner wants more, or when a source explains a step better than chat can. One at a time, only when it earns its place. Only entries in the library or ones you just saved with add_resource.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "id": {
            "description": "The entry id, or the first characters of it.",
            "type": "string"
          },
          "title": {
            "description": "Or the entry title (or part of it).",
            "type": "string"
          },
          "why": {
            "type": "string",
            "description": "One or two sentences, to the learner: what this gives them that the lesson did not."
          },
          "where": {
            "description": "Where to look: \"chapter 3\", \"from 12:40\", \"the section on invariants\".",
            "type": "string"
          },
          "node_id": {
            "description": "The plan node it deepens, if any.",
            "type": "string"
          }
        },
        "required": [
          "why"
        ],
        "additionalProperties": false
      }
    },
    {
      "name": "add_resource",
      "description": "Save a source to the learner's library for later: a URL you found with a web search or read (the page is fetched and its text kept), with a one-sentence note on why and a few tags. Sparingly: one or two per lesson, and only sources you actually read. A URL already on the shelf is not duplicated; your note and tags are merged in.",
      "input_schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "The page, video, paper or book to save."
          },
          "title": {
            "description": "Override the fetched title.",
            "type": "string"
          },
          "kind": {
            "description": "Guessed from the URL when omitted.",
            "type": "string",
            "enum": [
              "article",
              "video",
              "book",
              "paper",
              "course",
              "note"
            ]
          },
          "author": {
            "type": "string"
          },
          "note": {
            "type": "string",
            "description": "One sentence, to the learner: why this is worth keeping."
          },
          "tags": {
            "description": "A few lowercase tags, e.g. [\"calculus\", \"visual\"].",
            "maxItems": 8,
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "url",
          "note"
        ],
        "additionalProperties": false
      }
    }
  ]
}
